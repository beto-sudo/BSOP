import { PlaceholderSection } from '@/components/ui';

export default function DocumentsPage() {
  return (
    <PlaceholderSection
      icon="📄"
      title="Documents — Coming soon"
      description="A clean document layer for important files, quick retrieval, and eventually cross-system references across the operating stack."
    />
  );
}
