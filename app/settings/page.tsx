import { PlaceholderSection } from '@/components/ui';

export default function SettingsPage() {
  return (
    <PlaceholderSection
      icon="⚙️"
      title="Settings — Coming soon"
      description="Preferences, integrations, display controls, and future operating toggles for BSOP will be managed here."
    />
  );
}
