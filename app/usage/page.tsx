import { BarChart3, Bot, Coins, Cpu, DollarSign, Gauge, Wrench } from 'lucide-react';
import usage from '@/data/usage.json';
import { ActionLink, SectionHeading, Shell, Surface } from '@/components/ui';

const money=(value:number)=>value.toLocaleString('en-US',{style:'currency',currency:'USD',minimumFractionDigits:value<1?4:2,maximumFractionDigits:value<1?4:2});
const int=(value:number)=>value.toLocaleString('en-US');
const pct=(value:number)=>`${(value*100).toFixed(1)}%`;
const maxTrend=Math.max(...usage.dailyTrend.map(item=>item.cost),0.0001);

export default function UsagePage(){return <Shell><SectionHeading eyebrow="Usage" title="OpenClaw usage dashboard" copy={`Static telemetry generated from local OpenClaw transcripts (${usage.source.filesParsed} files parsed, latest activity ${usage.snapshot.date}).`} /><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-6">{[{icon:DollarSign,label:'Snapshot cost',value:money(usage.snapshot.totalCost),sub:`Latest active day: ${usage.snapshot.date}`},{icon:Cpu,label:'Snapshot tokens',value:int(usage.snapshot.totalTokens),sub:`${usage.snapshot.sessionCount} sessions captured`},{icon:Bot,label:'Messages',value:int(usage.snapshot.messages),sub:`${usage.snapshot.userMessages} user · ${usage.snapshot.assistantMessages} assistant`},{icon:Wrench,label:'Tool calls',value:int(usage.snapshot.toolCalls),sub:`${usage.summary.toolResults} tool results in full history`},{icon:Gauge,label:'Cache hit rate',value:pct(usage.usageTotals.cacheHitRate),sub:'Across all parsed assistant usage'},{icon:Coins,label:'History cost',value:money(usage.summary.totalCost),sub:`${usage.summary.sessionCount} sessions across all counted transcripts`}].map(item=><Surface key={item.label} className="p-5"><item.icon className="h-5 w-5 text-amber-300" /><div className="mt-4 text-xs uppercase tracking-[0.24em] text-white/40">{item.label}</div><div className="mt-2 text-2xl font-semibold text-white">{item.value}</div><div className="mt-2 text-sm text-white/55">{item.sub}</div></Surface>)}</div><section className="mt-10 grid gap-6 xl:grid-cols-[1fr_1fr]"><Surface className="p-6"><div className="mb-4 flex items-center gap-3 text-white"><Coins className="h-5 w-5 text-amber-300" /><h2 className="text-lg font-semibold">Snapshot cost by model</h2></div><div className="space-y-4">{usage.snapshot.costByModel.map(item=>{const width=usage.snapshot.totalCost?`${(item.cost/usage.snapshot.totalCost)*100}%`:'0%';return <div key={item.model} className="rounded-3xl border border-white/8 bg-white/4 p-4"><div className="flex items-center justify-between gap-4"><div><div className="text-sm font-semibold text-white">{item.label}</div><div className="mt-1 text-xs text-white/45">{item.model} · {item.provider}</div></div><div className="text-right"><div className="text-sm font-semibold text-amber-300">{item.formattedCost}</div><div className="mt-1 text-xs text-white/45">{int(item.messages)} msgs · {int(item.tokens)} tokens</div></div></div><div className="mt-3 h-2 rounded-full bg-white/8"><div className="h-2 rounded-full bg-amber-300" style={{width}} /></div></div>;})}</div></Surface><Surface className="p-6"><div className="mb-4 flex items-center gap-3 text-white"><Cpu className="h-5 w-5 text-amber-300" /><h2 className="text-lg font-semibold">Snapshot cost by provider</h2></div><div className="space-y-4">{usage.snapshot.costByProvider.map(item=>{const width=usage.snapshot.totalCost?`${(item.cost/usage.snapshot.totalCost)*100}%`:'0%';return <div key={item.provider} className="rounded-3xl border border-white/8 bg-white/4 p-4"><div className="flex items-center justify-between gap-4"><div className="text-sm font-semibold text-white">{item.provider}</div><div className="text-right"><div className="text-sm font-semibold text-amber-300">{item.formattedCost}</div><div className="mt-1 text-xs text-white/45">{int(item.messages)} msgs · {int(item.tokens)} tokens</div></div></div><div className="mt-3 h-2 rounded-full bg-white/8"><div className="h-2 rounded-full bg-emerald-300" style={{width}} /></div></div>;})}</div></Surface></section><section className="mt-10 grid gap-6 xl:grid-cols-[1.05fr_1.35fr]"><Surface className="p-6"><div className="mb-4 flex items-center gap-3 text-white"><BarChart3 className="h-5 w-5 text-amber-300" /><h2 className="text-lg font-semibold">Daily cost trend</h2></div><div className="space-y-4">{usage.dailyTrend.map(item=>{const width=Math.max((item.cost/maxTrend)*100,6);return <div key={item.date} className="grid grid-cols-[88px_1fr_auto] items-center gap-4"><div className="text-sm text-white/55">{item.date.slice(5)}</div><div className="flex items-end"><div className="w-full rounded-full bg-white/8"><div className="rounded-full bg-gradient-to-r from-amber-400 to-amber-300" style={{width:`${width}%`,height:'12px'}} /></div></div><div className="text-sm text-white/75">{item.formattedCost}</div></div>;})}</div></Surface><Surface className="overflow-hidden p-6"><div className="mb-4 flex items-center justify-between gap-4"><div><h2 className="text-lg font-semibold text-white">Recent sessions</h2><p className="mt-1 text-sm text-white/55">Latest 20 session transcripts, including reset/deleted session artifacts that still count toward usage.</p></div><ActionLink href="/agents" label="Open agent operations" /></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="text-white/45"><tr className="border-b border-white/8"><th className="pb-3 pr-4 font-medium">Started</th><th className="pb-3 pr-4 font-medium">Model</th><th className="pb-3 pr-4 font-medium">Msgs</th><th className="pb-3 pr-4 font-medium">Tokens</th><th className="pb-3 pr-4 font-medium">Cost</th><th className="pb-3 font-medium">Duration</th></tr></thead><tbody>{usage.recentSessions.map(item=><tr key={item.id} className="border-b border-white/6 text-white/78 last:border-0"><td className="py-3 pr-4 text-white/55">{item.timestamp.slice(0,16).replace('T',' ')}</td><td className="py-3 pr-4">{item.modelLabel}</td><td className="py-3 pr-4">{int(item.messages)}</td><td className="py-3 pr-4">{int(item.totalTokens)}</td><td className="py-3 pr-4 text-amber-300">{item.formattedCost}</td><td className="py-3">{item.durationMinutes.toFixed(1)}m</td></tr>)}</tbody></table></div></Surface></section></Shell>;}
