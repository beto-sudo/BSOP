# Iniciativa — Cuadratura del sobreprecio y cheque a notaría (DILESA)

**Slug:** `dilesa-cuadratura-sobreprecio`
**Empresas:** DILESA
**Schemas afectados:** `dilesa.ventas` — **campos nuevos de desglose** (promoción de gastos de escrituración, sobreprecio para gastos / productos adicionales, aportación Dilesa, enganche aplicado a gastos) separados del actual `descuento_total` que hoy los mezcla; motor `lib/dilesa/cuadratura.ts` + UI `components/dilesa/cuadratura-panel.tsx` + armado de inputs en `app/dilesa/ventas/[id]/page.tsx` y la captura de fase 10 (`.../capturar/10-firmas-programadas/page.tsx`). Lectura de `tipos_credito` (apoyo Infonavit, costo_venta_adicional), `productos.valor_comercial_referencia`, `desglose_precio`, `erp.cxc_pagos`. Posible ADR del modelo financiero.
**Estado:** in_progress
**Próximo hito:** Sprint **sobreprecio dividido** (separar `productos_adicionales` real del `sobreprecio_gastos_escrituracion`): PR 1 schema+motor+backfill ✅ ([#991](https://github.com/beto-sudo/BSOP/pull/991)) + PR 2 captura en la asignación ✅ ([#993](https://github.com/beto-sudo/BSOP/pull/993)); **siguiente: PR 3 — ajuste en fase 8** (dictaminación: corregir crédito/gastos/sobreprecio con el Anexo B). Pendientes de fondo: 3 anomalías DB↔PDF (M10-L8, M12-L9, M12-L33, Reminder `Claude 🧭`).
**Dueño:** Beto
**Creada:** 2026-06-17
**Última actualización:** 2026-06-23 (PR 2 captura del sobreprecio en la asignación [#993] + hotfix del overload de `fn_calcular_precio_venta` [#992]; pantalla con los 2 renglones + guía "hasta $X" + PDF/detalle desglosados; siguiente: ajuste en fase 8)

> Detonante operativo: la venta **MAYRA ALEJANDRA GOMEZ TERRAZAS** (FOVISSSTE Tradicional) se regresó de fase buscando "capturar un pagaré para cuadrar". El pagaré era una pista falsa al inicio (ver [PR #927](https://github.com/beto-sudo/BSOP/pull/927), chip fantasma) pero **resultó real** (9,387). El problema de fondo: la cuadratura mezcla en `descuento` cosas que son conceptualmente distintas (promoción vs sobreprecio), y eso descuadra la utilidad. Modelo en memoria `reference_dilesa_sobreprecio_cheque_notaria`.

## Problema

La cuadratura de gastos de escrituración tiene **cuatro fuentes de fondeo** que el modelo de BSOP NO separa: hoy las mete todas en `descuento_total`. Eso (a) hace cuadrar el saldo pero (b) **subestima la utilidad/participación de DILESA** (parece que regaló dinero que en realidad pagó el cliente vía el crédito), y (c) confunde al operador (caso MAYRA). El motor además distorsiona los derivados antes de escriturar (`chequeNotariaCalculado = min(gastosNetos, excedenteDisponible)` en [`cuadratura.ts:226`](../../lib/dilesa/cuadratura.ts) → `valorRealVentaDilesa` negativo, `montoNotaCredito` inflada).

## Modelo de Coda (validado con el Excel maestro "Relación Gastos Escrituración y Participación Dilesa", 2026-06-17)

Fórmulas reales de la hoja LDLE (columnas):

| Col | Concepto                                                       | Fórmula Coda                           |
| --- | -------------------------------------------------------------- | -------------------------------------- |
| M   | Saldo Cliente Dilesa                                           | `Precio − Crédito − Enganche`          |
| Q   | A pagar a Notaría                                              | `Gastos Esc. − Apoyo Infonavit`        |
| R   | Máxima Aportación Dilesa a Notaría (= **promoción** de gastos) | **15,000 fijo**                        |
| S   | Disponible Dilesa + enganche                                   | `−M + R`                               |
| L   | **Pagaré**                                                     | `Q − S` (el faltante)                  |
| T   | Cheque a notaría                                               | `= Q` (siempre cubre los gastos netos) |
| V   | Saldo cliente a notaría                                        | `Q − T = 0` ✓                          |

**Las 4 fuentes que cubren los gastos de escrituración** (ejemplo MAYRA, gastos 84,038):

| Fuente                              |      Monto | Naturaleza                                | Le cuesta a                |
| ----------------------------------- | ---------: | ----------------------------------------- | -------------------------- |
| Promoción DILESA (gastos esc.)      |     15,000 | Descuento comercial real                  | **DILESA** (baja utilidad) |
| Enganche / depósitos del cliente    |     35,000 | Pago del cliente                          | Cliente                    |
| Sobreprecio (productos adicionales) |     24,651 | Inflado en el precio → lo paga el crédito | Cliente (vía crédito)      |
| Pagaré del cliente                  |      9,387 | Deuda firmada a DILESA                    | Cliente                    |
| **Total = cheque a notaría**        | **84,038** |                                           |                            |

Regla (Beto): los **15,000 son la promoción estándar** de gastos de escrituración; cuando los gastos exceden promoción + enganche, el faltante se cubre **subiendo el precio con sobreprecio (productos adicionales)** para que lo pague el crédito de la institución, y el residual queda como **pagaré** del cliente. El apoyo Infonavit (30,000, solo créditos Infonavit) y el sobreprecio +6% (solo FOVISSSTE/IMSS) son **mutuamente excluyentes**.

**Mapeo Coda → BSOP (estado actual):**

- Coda `R` (promoción 15,000) **+** sobreprecio para gastos → hoy **ambos** caen en `descuento_total`/`descuento_gastos_escrituracion`. ⚠️ Hay que **separarlos**.
- Coda `L` (pagaré) → `monto_credito_directo`. ✓
- Coda `T` (cheque = gastos − apoyo) → `monto_cheque_notaria` (captura fase 11). ✓
- Coda `Q` apoyo → `tipos_credito.apoyo_infonavit_monto`. ✓

**Inconsistencias detectadas en el Excel de Coda:** la fórmula de `M` varía entre filas (a veces `−Enganche`, a veces `−Pagaré`, a veces ambos) — captura manual con errores. BSOP debe ser consistente.

## Outcome esperado

La cuadratura **desglosa completamente** las fuentes de cobertura de gastos (promoción, enganche, sobreprecio, apoyo, pagaré), de modo que: el saldo cuadra, **la utilidad/participación de DILESA sale correcta** (solo la promoción le cuesta, no el sobreprecio), y el operador identifica fácilmente "qué es de qué". Se acaban los derivados absurdos pre-escrituración.

## Alcance

**Sprint 1 — Diseño** ✅ — [ADR-045](../adr/045_cuadratura_desglose_gastos_escrituracion.md) (modelo de 4 fuentes, spec de campos, fórmulas) + mockup del panel. Aprobado por Beto.

**Sprint 2 — Implementación (PRs chiquitos):**

- **PR a — columnas** ✅ ([#934](https://github.com/beto-sudo/BSOP/pull/934)): `precio_base`, `incremento_credito`, `sobreprecio_adicionales`, `promocion_gastos_monto` (nullable) en `dilesa.ventas`.
- **PR b — motor con fallback**: `lib/dilesa/cuadratura.ts` lee el desglose si está poblado, si no usa el modelo viejo (D7). Verificación obligatoria de que las ~230 escrituradas dan idéntico. Tests.
- **PR c — captura + panel**: asignación pobla las 4 columnas (nuevas ventas); fase 10 captura el pagaré por faltante de gastos; panel muestra las 4 fuentes (como el mockup).

**Alcance del poblado (acotado — decisión Beto):** NO backfill masivo. Las cerradas/legacy se quedan como están (fallback). Solo se pobla en **nuevas asignaciones** (el flujo lo hace) y en las **~70 activas en proceso** conforme se operan. MAYRA es la primera.

**Sprint 3 — Cerrar MAYRA** con el modelo desglosado (en pausa hasta tener motor + captura).

## Riesgos

- **Tocar el saldo rompe ventas que hoy cuadran.** Cualquier cambio a `saldoCliente`/`cubierta` exige verificación adversarial contra las ~230 escrituradas antes de mergear.
- **Histórico protegido por fallback (no backfill):** las cerradas/legacy quedan con las columnas en `null` y el motor usa el modelo viejo — no se reescribe nada cerrado. El riesgo se reduce a verificar que el fallback dé idéntico en las escrituradas.
- **Modelo financiero sensible.** Afecta correo al Consejo, copiloto de cierre, nota de crédito, utilidad Dilesa. Nada al motor sin diseño aprobado por Beto.

## Métricas de éxito

- Las 4 fuentes de cobertura de gastos visibles y sumando al cheque a notaría en cada venta.
- Utilidad/participación de DILESA correcta (solo la promoción le cuesta, no el sobreprecio).
- 0 derivados absurdos pre-escrituración.
- Las ventas que hoy cuadran siguen cuadrando.

## Bitácora

- **2026-06-17** — Promovida. Detonante: venta MAYRA. Análisis multi-agente confirmó el modelo de sobreprecio/cheque. [PR #927](https://github.com/beto-sudo/BSOP/pull/927) (chip fantasma + rol del pagaré) mergeado. [PR #928](https://github.com/beto-sudo/BSOP/pull/928) (promoción de la iniciativa) mergeado. Memoria `reference_dilesa_sobreprecio_cheque_notaria`.
- **2026-06-17** — Beto pasó el **Excel maestro de cuadraturas de Coda** ("Relación Gastos Escrituración y Participación Dilesa"). Validadas las fórmulas (hoja LDLE, 302 ventas): el modelo gira en torno a los gastos de notaría, el cheque = gastos − apoyo, el pagaré = faltante. **Hallazgo clave:** el "descuento" de BSOP mezcla la **promoción** (15,000, costo de DILESA) con el **sobreprecio** (productos adicionales, lo paga el crédito) — hay que desglosarlos para que la utilidad salga bien. MAYRA: 84,038 gastos = promoción 15,000 + enganche 35,000 + sobreprecio 24,651 + pagaré 9,387.
- **2026-06-17** — Beto: **arrancar rediseño** (iniciativa a `in_progress`, diseño antes de código) y **no cerrar MAYRA** hasta tener los campos desglosados. El PR #929 (fix rápido de fase 10 que metía el sobreprecio en el descuento) se **cerró** y se pliega al rediseño con la fórmula desglosada correcta.
- **2026-06-17** — Beto pasó el **desglose de condiciones finales de MAYRA** (cadena base 899,000 → +FOVISSSTE 55,419 → interno 954,419 → +adicionales 24,651 → escrituración 979,070; presupuesto notarial 84,038 = promoción 15,000 + enganche 35,000 + adicionales 24,651 + pagaré 9,387). Aprobó el **mockup del panel** desglosado. Sprint 1 entregado: **[ADR-045](../adr/045_cuadratura_desglose_gastos_escrituracion.md)** con el modelo de las 4 fuentes, la spec de campos (hogar de cada uno: `desglose_precio` jsonb para la cadena de precio, `descuento_gastos_escrituracion` = solo promoción, `productos_adicionales` = sobreprecio, `cxc_pagos` = enganche, `monto_credito_directo` = pagaré, `monto_cheque_notaria` = cheque) y el plan de migración. **Corrección vs lo dicho antes:** el descuento de MAYRA se queda en **15,000** (la promoción), NO se sube a 39,651 — el sobreprecio (24,651) vive aparte en el precio. Pendiente: aprobación de Beto para arrancar Sprint 2 (código).
- **2026-06-17 (tarde)** — Sprint 2 ejecutado: motor con fallback + UI coherente en **[PR #938](https://github.com/beto-sudo/BSOP/pull/938)** (rama `claude/dilesa-cuadratura-s2-reconcilia`). MAYRA cuadra al peso y Beto la aprobó ("Ahora sí me cuadra y se ve muy bien"). Se eliminó el header con −74,651 (dos cuentas: precio cubierto por crédito + pagaré del cliente). Columna duplicada `sobreprecio_adicionales` dropeada → se usa `productos_adicionales`.
- **2026-06-17 (tarde)** — Pregunta de Beto: ¿cómo se ven las otras activas? **Análisis:** de 101 activas (fase 1-15), 13 FOVISSSTE / 88 INFONAVIT. Reconciliados 2 INFONAVIT al peso. **Beto confirmó el modelo unificado:** es UN caso con 2 parámetros (FOVISSSTE +6%/sin subsidio; INFONAVIT +0%/subsidio 30k), DILESA solo aporta 15,000, el resto se cubre subiendo el precio (sobreprecio) o pago directo del cliente. **Investigación Coda:** NO tiene columna `Precio Base` (el 899k era desglose manual de Beto); el ancla real es `valor_escrituracion` + `productos_adicionales`, ya en BSOP. **Decisión de población (Beto):** tabla para que **Edgar** capture los datos reales (no derivar, no Coda). Construido el **Google Sheet** `1sGjtWZN3ggFJ0IKvBJVlto-M37kh_sQCIUuf8JDS7us` (101 activas, base INFONAVIT pre-llena, FOVISSSTE en blanco, columnas de captura marcadas, pestaña de instrucciones). Pendiente: Edgar llena → re-import a prod (escritura financiera, requiere OK de Beto).
- **2026-06-18** — **Los PDFs de Solicitud de Asignación (Coda `c-j4ZhKyzB8g`) revelaron la fórmula exacta** → ya no hace falta captura de Edgar (tabla obsoleta). El +6% es sobre (base+geom+sobreprecio): `escrituración = (base+geom+sobreprecio)×1.06`, así que `precio_base = escr/(1+pct6) − sobreprecio` e `incremento = escr×pct6/(1+pct6)`. Validado 92/92. **Decisión Beto:** geometría EXACTA de los PDFs (no recalcular — el catálogo cambió desde la venta; para que BSOP quede idéntico a lo firmado) y **desglosada** (excedente/frente verde/esquina/venta futuro por separado). **Sprint geometría:** migración 4 columnas (`valor_excedente_terreno/frente_verde/esquina/venta_futuro`, aplicada a prod) + motor (`formacionPrecio` con líneas de geometría, `precioInterno = base+geom+incremento`) + panel (líneas condicionales) + workflow que extrajo los 26 PDFs (18 cuadran + 2 fix de extracción = 20 exactos; **6 anomalías DB↔PDF**: M10-L8/M12-L9/M9-L18 descuentos no capturados, M8-L10 escrituración sin sobreprecio, M22-L1 +3k, M9-L18 duplicado). Script `populate_dilesa_desglose_precio.ts` (DRY_RUN cuadra 86/86: 20 PDF + 66 derivado; 6 omitidas). **Pendiente: OK explícito de Beto para escribir las 86** (el clasificador bloqueó el write financiero) + decisión sobre las 6 anomalías.
- **2026-06-18 (poblado aplicado)** — Beto resolvió las anomalías: M8-L10 escrituración corregida al PDF (3,217,150 + sobreprecio 163,500), M22-L1 → 1,392,050, M9-L18 duplicado → soft-delete de la hipotecaria (queda Tafoya). **88 pobladas en prod** (cuadran al peso; FOVISSSTE sin geometría se redondea la base al entero del PDF — MAYRA 899,000 exacto). **Hallazgo M12-L33:** su PDF reveló que la DB puede DISCREPAR del PDF firmado aun en lotes "limpios" — escrituración DB 1,021,000 vs PDF 1,027,390 (+ venta futuro 8,990 no capturada). M12-L33 pasó a anomalía pendiente (ahora 3: M10-L8, M12-L9, M12-L33). **Implicación abierta:** para garantizar "cero diferencias vs PDF" en TODAS habría que verificar las 88 contra sus PDFs (Beto decidió NO — acepta la derivación para INFONAVIT, revisa anomalías conforme salgan). Código en [PR #942](https://github.com/beto-sudo/BSOP/pull/942).
- **2026-06-18 (cierre del problema original + pulido)** — **[PR #944](https://github.com/beto-sudo/BSOP/pull/944) (merged):** la fase 10 (Programación de Firmas) ya muestra la sección del pagaré por el faltante de GASTOS (motor `coberturaGastos.pagareNecesario` vía `useVentaResumen`), no el saldo de precio — MAYRA aparece con $9,387 y se activa el candado de cierre. Cierra el problema con el que arrancó la sesión. **[PR #945](https://github.com/beto-sudo/BSOP/pull/945) (merged):** editor de descuentos (`cuadratura-ajustes.tsx`) por modelo — con desglose el descuento es read-only (promoción + sobreprecio, los buckets viejos no aplican); legacy auto-calcula el total como suma de buckets. Detonante: Beto notó el total capturable que debía auto-sumarse. **Pendientes vivos:** 3 anomalías (Reminder) + la raíz (flujo de asignación).
- **2026-06-18 (la raíz)** — **[PR #947](https://github.com/beto-sudo/BSOP/pull/947) (merged):** la asignación (`nueva/page.tsx`) ahora persiste el desglose ESCALAR (precio_base, incremento, geometría, promoción) desde `fn_calcular_precio_venta`, no solo el snapshot `desglose_precio` jsonb. Las ventas nuevas nacen con el modelo desglosado, sin backfill.
- **2026-06-18 (NC y valor real netos del cheque)** — Michelle reportó que la Nota de Crédito no cuadraba. Revisados ambos archivos operativos (Michelle "Notas de crédito" + Ale "Relación Gastos Escrituración y Participación"). **[PR #951](https://github.com/beto-sudo/BSOP/pull/951) (merged):** alineado el motor a su fórmula — **Valor Real = detonación (`monto_detonado` ?? crédito) + enganche − cheque + pagaré**; **NC = Facturado − Valor Real** (incluye el cheque); **descuento real = Escritura − Valor Real** (promoción + sobreprecio, no solo la promo); **comisiones sobre Valor Real − productos adicionales**. Verificado al peso: 16/16 filas detonadas de junio (Δ=0.00). Impacto: MAYRA NC 35,000→74,651, valor real →939,419, descuento real →39,651. También se restauró la geometría (#942) en `venta-detalle/provider.tsx` (el refactor a tabs la había dejado fuera).
- **2026-06-18 (Máxima Aportación — pendiente, modelo aclarado por Beto)** — la "Máxima Aportación Dilesa a Notaría" de Ale NO está formulada (captura manual). Debe ser **promoción existente (15,000) + el sobreprecio hecho específicamente para cubrir gastos**. El sobreprecio nace cuando el **monto del crédito está por encima del valor de la vivienda** → se sube el precio para que el crédito absorba los gastos. **Implementación propuesta: capturarlo desde la Solicitud de Asignación por el vendedor** (que ya conoce el monto del crédito autorizado). Tarea separada — reconcilia la card "Cobertura del presupuesto notarial".
- **2026-06-18 (cards de descuento desglosado + cobertura cuadra)** — **[PR #955](https://github.com/beto-sudo/BSOP/pull/955):** a partir de la operación de **Luis Gerardo Arizpe**, reencuadradas las dos cards del modelo desglosado. (1) "Descuento de la operación": el **descuento real** (= Escrituración − Valor Real, columna "Descuento" de Michelle) se **parte en `Descuento por promoción`** (el bono autorizado del catálogo, topado al máximo — Arizpe usa los 15,000) **+ `Descuento por sobreprecio`** (el resto — Arizpe 3,313, MAYRA 24,651) **= `Descuento total`** (amarra al "Descuento real" de la card de Cierre). La nota señala cuánto del sobreprecio falta formalizar como Máxima Aportación (Arizpe: 3,313 sin capturar como productos; MAYRA: 0, ya capturado). Apoyo Infonavit separado como subsidio. (2) "Cobertura del presupuesto notarial" — **rehecha (feedback de Beto):** ahora plasma el cálculo COMPLETO sin inferir nada del subsidio. Jala los **gastos notariales brutos** (Arizpe 48,313), pinta el **subsidio Infonavit** como línea propia si lo hay (30,000), y desglosa el resto: `(−) Aportación DILESA (promoción)` 15,000 + `(−) Enganche` + `(−) Sobreprecio` 3,313 + `(−) Pagaré` → **cuadra exacto en 0**. El split promoción/sobreprecio del lado DILESA usa el helper `partirDescuento` (motor, exportado y testeado) sobre el faltante de gastos; la card de descuento usa el mismo helper sobre el descuento real. Motor: se agregó `coberturaGastos.gastosBrutos` y se retiraron `aportacionDilesa`/`saldoCobertura` (los reemplaza el cálculo transparente en el panel). **Última revisada:** la nota al pie del panel es mode-aware (Michelle/Ale para desglose; Coda para legacy) y el header del motor documenta los dos modelos. 41 tests del motor (+6 de `partirDescuento` y cobertura). Sin auto-merge — Beto revisa el preview. **Sigue:** la Máxima Aportación desde la captura de la solicitud (formaliza el sobreprecio derivado — Arizpe muestra 3,313 sin capturar).
- **2026-06-18 (mini-resumen ligado a la cuadratura — una sola fuente)** — Beto detectó que el mini-resumen (cabecera del expediente) mostraba "Pagaré del cliente $3,313" para Arizpe, que tiene pagaré real $0 — sacaba el dato de `pagareNecesario` (el faltante TEÓRICO si DILESA solo diera la promo), no del pagaré real. **Fix (sigue en #955):** el split de cobertura (promoción usada + sobreprecio + saldo) se centralizó en el **motor** (`coberturaGastos.aportacionPromocion`/`sobreprecioCobertura`/`saldoCobertura`, vía `partirDescuento`) — fuente ÚNICA que leen la card de cuadratura Y el mini-resumen, sin recalcular en cada componente. El mini-resumen ya no muestra el pagaré teórico: pinta el estado de **Gastos notariales** (Cubierto ✓ ligado a `saldoCobertura`), espejo de "Precio Cubierto ✓"; el pagaré real sigue en su columna "Crédito directo (pagaré)". Raíz de la queja "se arregla en un lado y no en otro": cada superficie recalculaba; ahora todas leen del motor. 40 tests.
- **2026-06-18 (copiloto de cierre ligado a la cuadratura)** — Beto vio el mismo fantasma en el **copiloto de cierre**: "Cuadratura cubierta — Saldo del cliente: $3,313" marcaba la condición como NO cumplida para Arizpe (que SÍ está cubierta). El copiloto leía `cuadratura.saldoCliente`/`cubierta` **legacy** (en desglose = cheque 18,313 − descuento 15,000 = 3,313, un saldo fantasma que mezcla precio + descuento). **Fix (sigue en #955):** el motor expone **`operacionCubierta`** (model-aware: desglose ⇒ crédito cubre el precio Y las fuentes cubren el presupuesto; legacy ⇒ = `cubierta`) y **`saldoOperacion`** (el residual real a mostrar). El provider y la página de Fase 17 (operación terminada) ahora pasan esos campos a `evaluarCierre`. Arizpe queda "Cuadratura cubierta ✓" sin saldo. 41 tests (+1 que fija el caso). Mismo principio: una sola fuente en el motor.
- **2026-06-19 (backfill de 6 nativas del hueco #942↔#947)** — Revisando el correo al Consejo (las fases tempranas del pipeline mostraban $0), salió un segundo hallazgo: **6 ventas activas nativas** (origen `asignacion`, `componentes_detallados=true`: 3 Asignada + 3 Formalizada) tenían el snapshot `desglose_precio` rico pero las **columnas escalares en NULL**. Cayeron en el hueco entre el poblado de las 88 ([#942](https://github.com/beto-sudo/BSOP/pull/942)) y el deploy del flujo que las persiste al asignar ([#947](https://github.com/beto-sudo/BSOP/pull/947)) — el script de #942 no las tocó y se crearon antes del deploy de #947. Sin `precio_base`, `tieneDesglose=false` → su cuadratura del sobreprecio caía a modelo legacy. **Migración `20260619142528`** (aplicada a prod con `db push`, OK de Beto): pobló `precio_base`/`incremento_credito`/geometría/`promocion_gastos_monto` **desde el snapshot congelado** (no re-tarifa — regla de no recalcular en vivo). Verificado al peso: `precio_base + geometría + incremento + productos_adicionales = precio_asignacion` en las 6; `precio_asignacion`/`valor_escrituracion`/pagos intactos. Idempotente y Preview-safe. Alineado con el alcance acotado (activas en proceso, no las 627 cerradas).
- **2026-06-22 (enganche doble-contado + gastos heredados de Coda)** — Detonante: la venta **M3-L9 (Juan Antonio, Infonavit Tradicional)** mostraba un "descuento por sobreprecio" de **$17,953** sin tener `productos_adicionales` y un saldo de cobertura de **−$124,782**. Diagnóstico (verificado contra prod): **dos causas que se combinan**. (1) **Dato de gastos heredado de Coda:** `gastos_escrituracion` = 62,161 venía de la migración; el **Anexo B real** (adjunto `condiciones_financieras`, extraído por IA el 19-jun pero nunca aplicado a la columna porque la venta saltó de Coda a fase 9 sin pasar por la captura de fase 8) dice **42,569.42** (titulación 12,569.42 + impuestos/derechos 30,000). (2) **Bug del modelo:** el motor restaba el enganche del cliente de los gastos aunque en Infonavit con crédito < precio el enganche cubre el **precio**, no los gastos → doble conteo. **A+B (data-fix, OK de Beto):** UPDATE `gastos_escrituracion` 62,161→42,569.42 (M3-L9) y 44,208→42,569.42 (M10-L28) en prod, desde el Anexo B ya extraído. **C (motor):** el enganche se aplica primero al saldo del precio, solo el excedente fondea gastos ([ADR-045 D8](../adr/045_cuadratura_desglose_gastos_escrituracion.md)); `coberturaGastos` expone `engancheCliente` (a gastos) + `engancheAlPrecio` (al precio, nota del panel); `round2` normaliza −0; nota en `cuadratura-panel.tsx`. **Verificación adversarial:** cero FOVISSSTE afectadas (su crédito cubre el precio); 12 Infonavit (11 activas + terminada M4-L20) pasan de saldo negativo a 0; M3-L9 cuadra exacto (precio = crédito 762,265 + enganche 156,943 [faltan 792 por cobrar]; gastos 42,569 = subsidio 30,000 + DILESA 12,569 del bono; sobreprecio 0). 43 tests del motor verdes (+2 casos Infonavit). **Hallazgo aparte (no en este PR):** la terminada **M9-L17-LDS** (Hipotecario) sigue descuadrada por un enganche de 2,659,480 sobre una casa de 2,709,480 — probable disposición de crédito mal etiquetada `fuente='cliente'` (doble conteo de `posibleDobleConteo`), a auditar.
- **2026-06-22 (investigación a fondo + limpieza — el modelo correcto es bono+sobreprecio)** — Beto pidió repasar TODA la cuadratura contra Ale/Michelle. Se exploró un rediseño (medir el descuento como aportación DILESA + faltante del peculio del cliente) y se investigaron a fondo el **Anexo B** + los **4 manuales oficiales de INFONAVIT** (Lineamientos de Titulación, Políticas de Avalúos, Reglas DOF, Impuestos y Derechos Coahuila — guardados en [`docs/reference/infonavit/`](../reference/infonavit/)). Confirmado por los manuales: los gastos (titulación + impuestos + avalúo) son a cargo del trabajador pero **objeto del crédito** (Crédito Otorgado = Crédito Neto + gastos); el **avalúo lo paga el cliente a la valuadora** (financiado por el crédito); los **$30k son Impuestos y Derechos de Coahuila**, no un subsidio. **PERO la verificación contra las NC detonadas de Michelle DESCARTÓ el rediseño:** medir el descuento como "solo el faltante del peculio" daba **0 de descuento, off por $13k–$18k** vs lo que Michelle valida. **Beto aclaró la pieza clave:** cuando el descuento real rebasa **$15,000** es porque se manejó un **sobreprecio** para cubrir el diferencial. → **El modelo correcto es el de siempre: `descuento real = Escritura − Valor Real`, partido en promoción (bono ≤ 15,000) + sobreprecio (el exceso).** Se **limpió el [PR #978](https://github.com/beto-sudo/BSOP/pull/978)**: revertido el rediseño (aportación+pendiente) → de vuelta a promoción+sobreprecio; se conservaron el **data-fix A+B** (gastos del Anexo B, que SÍ resolvió M3-L9 — su descuento baja a 13,361 = todo bono, sobreprecio 0; el "$17,953 fantasma" era solo gastos inflados), el **fix del enganche-al-precio** (corrige el −124,782 de la card de cobertura) y el **fix de la etiqueta de facturación** ("neto = valor real, no escrituración"). 43 tests verdes.
- **2026-06-23 (sprint sobreprecio dividido — PR 1: schema + motor + backfill)** — Beto pidió arrancar el sprint que separa el sobreprecio para gastos de los productos adicionales reales (hoy revueltos en `productos_adicionales`). **Análisis de la captura:** `productos_adicionales` tenía DOS vidas — la pantalla de asignación lo capturaba como "productos reales (closets)" pero el motor lo trataba como "sobreprecio que fondea gastos y NO comisiona". **Dimensionamiento:** 56 ventas con monto; los grandes (122k patrón 899k→1,021k; outliers M4-L30 678k / M5-L16 560k contado) NO son productos — son sobreprecio de margen (subir el precio al monto del crédito) o bonificaciones altas en terminadas para mover inventario atorado (Beto). **Decisiones de Beto:** (1) el sobreprecio se captura en la asignación tras la precalificación (campo en blanco, guía "hasta $X" del margen del crédito, el vendedor lo teclea solo si hace falta; ajustable en fase 8 con el Anexo B); (2) los productos reales SÍ comisionan a la misma tasa; (3) **mover TODO `productos_adicionales` → `sobreprecio_gastos_escrituracion` en las 56** (no solo activas) para unificar — "ninguno de los que traen productos adicionales son realmente productos adicionales". **[PR #991](https://github.com/beto-sudo/BSOP/pull/991):** migración `20260623155819` (columna + backfill de 56, suma 6,320,874 = idéntica a la vieja de productos + `fn_calcular_precio_venta` con `p_sobreprecio_gastos_escrituracion`); motor `cuadratura.ts` (`sobreprecioGastos` fondea gastos/no comisiona, `productosAdicionales` comisiona, `formacionPrecio` con dos renglones); ensambladores + panel + 44 tests. **Verificado:** backfill exacto, RPC OK (920k+5k+10k=935k), CI verde (1978 tests). El backfill NO cambia ninguna cuadratura/comisión existente (solo re-etiqueta).
- **2026-06-23 (PR 2: captura del sobreprecio en la asignación + hotfix del overload)** — La pantalla de **Solicitud de Asignación** (`nueva/page.tsx`) ahora separa los dos renglones: "Productos adicionales" (extras reales, comisionan, ya no obligatorio) y "Sobreprecio para gastos de escrituración" (nuevo, en blanco por defecto, con guía **"hasta $X"** = margen del crédito sobre el valor de venta; no comisiona). `fn_calcular_precio_venta` recibe el sobreprecio y lo suma al precio; el preview, el **detalle** (`[id]/page.tsx`) y el **PDF de solicitud** (`solicitud-asignacion.tsx`) muestran ambos renglones desglosados. **[PR #993](https://github.com/beto-sudo/BSOP/pull/993)** (revisado por Beto en el Preview de Vercel, sin auto-merge; mockup + explicación al equipo de ventas afinada a la pantalla). **Hotfix [#992](https://github.com/beto-sudo/BSOP/pull/992):** la migración del PR 1 hizo `CREATE OR REPLACE` agregando un parámetro, lo que en PostgreSQL crea un **overload** (funciones de 5 y 6 params) en vez de reemplazar → PostgREST no podía resolver la llamada de la pantalla ("Could not choose the best candidate function"). Se dropeó la firma vieja de 5 params (la de 6, con el sexto `DEFAULT 0`, cubre a todos los callers); registrado para que el Preview branch no herede el overload. Detonante: Beto vio el error en "Cálculo de precio" al abrir la pantalla. Lección: una migración que cambia la firma de una función debe `DROP FUNCTION` la firma vieja antes del `CREATE OR REPLACE`.

## Decisiones registradas

- **2026-06-17** — El "descuento" se **desglosa** en conceptos separados: promoción de gastos (costo de DILESA) vs sobreprecio/productos adicionales (lo paga el crédito) vs descuento comercial al precio. Es la raíz de la subestimación de utilidad.
- **2026-06-17** — **Diseño antes de código** para el motor (decisión de Beto): Sprint 1 entrega documento + ADR revisable; nada al motor de cuadratura sin su OK.
- **2026-06-17** — MAYRA **en pausa** hasta el rediseño (no cerrarla con el descuento mezclado, para que la utilidad/participación salga correcta desde el inicio).
- **2026-06-17** — **Alcance acotado (decisión Beto):** NO backfill masivo de las 627. Las ventas ya cerradas (escrituradas/terminadas/desasignadas) se quedan como están; el motor las maneja con **fallback** (columnas null → modelo viejo). Solo se pobla el desglose en **nuevas asignaciones** (el flujo lo guarda) y en las **~70 activas en proceso** conforme se operan (MAYRA primero). Columnas DDL ya en prod ([#934](https://github.com/beto-sudo/BSOP/pull/934)). Esto elimina el riesgo de reescribir histórico.
- **2026-06-17 (tarde)** — **Modelo unificado (Beto):** FOVISSSTE y INFONAVIT son el MISMO modelo con 2 parámetros del catálogo `dilesa.tipos_credito` (`costo_venta_adicional_pct`: 6%/0; `apoyo_infonavit_monto`: 0/30k). DILESA aporta máximo 15,000; el faltante de gastos se cubre subiendo el precio (sobreprecio que absorbe el crédito) o con pago directo del cliente al notario.
- **2026-06-17 (tarde)** — **Población vía Edgar, no derivación ni Coda (Beto):** los datos reales (precio_base FOVISSSTE, promoción, gastos faltantes, enganche, pagaré) los captura Edgar en un Google Sheet; BSOP re-importa. Coda descartado como fuente: no tiene `Precio Base` y su cadena de catálogo sobrepasa el valor de escrituración real. El re-import es escritura financiera en prod → **OK explícito de Beto antes de correr**.
- **2026-06-22** — **El enganche se aplica primero al precio, solo el excedente a gastos** ([ADR-045 D8](../adr/045_cuadratura_desglose_gastos_escrituracion.md)). El modelo desglosado asumía (de MAYRA/FOVISSSTE) que el crédito cubre el precio y el enganche fondea los gastos; falso en Infonavit con crédito < precio, donde el enganche va al precio. Restarlo de los gastos era doble conteo. La corrección NO toca FOVISSSTE (crédito ≥ precio) ni los derivados de utilidad/NC/comisiones (esos sí usan el enganche completo, correctamente). Detonante: M3-L9.
- **2026-06-22** — **Los gastos de escrituración salen del Anexo B (dictaminación), no de Coda** (Beto). Las ventas migradas de Coda ya en fase avanzada (≥ 8) heredaron `gastos_escrituracion` de Coda sin pasar por la captura de fase 8 que aplica el valor extraído del Anexo B. Donde el Anexo B ya está extraído (`adjuntos.metadata.analisis_notarial`), el valor bueno es `gastos_titulacion + impuestos_derechos`. Se corrigió a mano M3-L9 y M10-L28 (las 2 activas con discrepancia material). Pendiente sistémico: que la app aplique el Anexo B extraído a la columna cuando una venta migrada saltó la fase 8.
- **2026-06-22** — **El descuento real = Escritura − Valor Real, partido en promoción (bono ≤ 15,000) + sobreprecio (el exceso)** (modelo de Michelle/Ale, RECONFIRMADO por Beto). Cuando el descuento rebasa $15,000 es porque se manejó un **sobreprecio**: se sube el precio para que el crédito absorba el diferencial. Un análisis intermedio que medía el descuento como "solo el faltante del peculio" se **descartó** tras verificarlo contra las NC detonadas de Michelle (daba 0, off por $13k–$18k). El **avalúo lo paga el cliente a la valuadora** (financiado por el crédito), fuera de la cuadratura de DILESA. La facturación es **proyección** hasta que se emita el CFDI; el "neto facturado" es el ingreso real (= escritura − descuento real), nunca "= escrituración".
- **2026-06-22 (pendiente — sobreprecio dividido)** — Beto: el sobreprecio para gastos hay que determinarlo **en la asignación** (cuando el monto de crédito del cliente supera el valor de venta y permite agregarlo). Hoy se mezcla en `productos_adicionales`; **separar** ese campo (productos reales) de un **campo nuevo de sobreprecio para gastos de escrituración**. Tarea de schema + flujo de asignación, a diseñar.
- **2026-06-23 (sobreprecio dividido — schema + comisiones)** — `productos_adicionales` se **separa** en dos columnas ([ADR-045 D9](../adr/045_cuadratura_desglose_gastos_escrituracion.md), migración `20260623155819`): `sobreprecio_gastos_escrituracion` (sube el precio para que el crédito absorba los gastos; **NO comisiona**, fondea gastos) y `productos_adicionales` (productos reales del paquete; **SÍ comisionan**). **Decisión Beto:** mover TODO el histórico (56 ventas, no solo las 22 activas) `productos_adicionales` → sobreprecio, porque "ninguno de los que traen productos adicionales son realmente productos adicionales". El backfill no cambia ninguna cuadratura/comisión (el motor trata el sobreprecio igual esté en un campo o el otro); solo re-etiqueta y libera `productos_adicionales` para productos reales. Los productos reales comisionan a la misma tasa (1%/1.5% Loma Verde). Captura: el sobreprecio es campo **en blanco** por defecto con guía "hasta $X" del margen del crédito (nunca auto-llenado); ajustable en fase 8 (dictaminación) con los gastos reales del Anexo B. Las **28 terminadas + 6 desasignadas** no se reabren (su backfill solo re-etiqueta, no toca su cuadratura).
