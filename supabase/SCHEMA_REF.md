# BSOP Supabase Schema Reference

<!--
  Auto-generated by `scripts/gen-schema-ref.ts`.
  Last regenerated: 2026-06-09T01:54:47Z
  Schemas: analytics, core, dilesa, erp, health, maquinaria, peptides, playtomic, public, rdb
  DO NOT EDIT BY HAND.
  Regenerate via: `npm run schema:ref` (requires SUPABASE_DB_URL).
-->

Referencia completa de tablas, vistas y columnas de BSOP en Supabase, tal como existen en el momento de la última regeneración. Para cambiar esta referencia, **aplica la migración correspondiente** y vuelve a correr `npm run schema:ref`.

**Schemas cubiertos:** `analytics` · `core` · `dilesa` · `erp` · `health` · `maquinaria` · `peptides` · `playtomic` · `public` · `rdb`

---

## Schema `analytics`

### `analytics.metric_dictionary`

> Definicion canonica de metricas. Una fila = una metrica con su formula.

- **metric_key** `text` NOT NULL — PK
- **display_name** `text` NOT NULL
- **descripcion** `text` NOT NULL
- **formula** `text`
- **source_mv** `text`
- **unidad** `text`
- **owner** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `analytics.mv_corte_diario` _(materialized view)_

> Una fila por corte de caja con totales, diferencias categorizadas, vouchers, gap voucher-vs-terminal.

_(sin columnas)_

### `analytics.mv_playtomic_ocupacion` _(materialized view)_

> Ocupacion por (cancha, fecha, hora) en TZ America/Matamoros.

_(sin columnas)_

---

## Schema `core`

### `core.audit_log`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **usuario_id** `uuid` — FK → `core.usuarios(id)`
- **accion** `text` NOT NULL
- **tabla** `text` NOT NULL
- **registro_id** `uuid`
- **datos_anteriores** `jsonb`
- **datos_nuevos** `jsonb`
- **ip_origen** `inet`
- **user_agent** `text`
- **created_at** `timestamp with time zone` DEFAULT `now()`

### `core.bancos`

> Catálogo seed de bancos/terminales para conciliación de vouchers de tarjeta. Cross-empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **codigo** `text` NOT NULL
- **nombre** `text` NOT NULL
- **patron_ocr** `text`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `core.empresa_documentos`

> Liga documentos legales (erp.documentos) a empresas con un rol semántico. Múltiples vigentes por rol; un default por (empresa_id, rol) usado por flujos automáticos como contratos LFT. Reemplaza la captura manual de jsonb en core.empresas.escritura_* — esos jsonb se mantienen como caché sincronizado vía trigger.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **documento_id** `uuid` NOT NULL — FK → `erp.documentos(id)`
- **rol** `text` NOT NULL
- **es_default** `boolean` NOT NULL DEFAULT `false`
- **asignado_por** `uuid` — FK → `core.usuarios(id)`
- **asignado_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `core.empresa_socios`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **familia** `text`
- **tipo** `text` NOT NULL DEFAULT `'entidad'::text`
- **socio_empresa_id** `uuid` — FK → `core.empresas(id)`
- **socio_persona_id** `uuid`
- **porcentaje** `numeric` NOT NULL
- **orden** `integer` NOT NULL DEFAULT `1`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `core.empresas`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **nombre** `text` NOT NULL
- **slug** `text` NOT NULL
- **activa** `boolean` DEFAULT `true`
- **logo_url** `text`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **header_url** `text`
- **rfc** `text`
- **razon_social** `text`
- **regimen_capital** `text`
- **nombre_comercial** `text`
- **fecha_inicio_operaciones** `date`
- **estatus_sat** `text`
- **id_cif** `text`
- **regimen_fiscal** `text`
- **domicilio_cp** `text`
- **domicilio_calle** `text`
- **domicilio_numero_ext** `text`
- **domicilio_numero_int** `text`
- **domicilio_colonia** `text`
- **domicilio_localidad** `text`
- **domicilio_municipio** `text`
- **domicilio_estado** `text`
- **actividades_economicas** `jsonb`
- **obligaciones_fiscales** `jsonb`
- **csf_fecha_emision** `date`
- **csf_url** `text`
- **registro_patronal_imss** `text`
- **representante_legal** `text`
- **escritura_constitutiva** `jsonb`
- **escritura_poder** `jsonb`
- **tipo_contribuyente** `text` NOT NULL DEFAULT `'persona_moral'::text`
- **curp** `text`
- **solo_fiscal** `boolean` NOT NULL DEFAULT `false`
- **email_fiscal** `text`
- **uso_cfdi_default** `text`
- **color_primario** `text`
- **color_primario_dark** `text`
- **color_secundario** `text`
- **color_texto_titulo** `text`
- **color_fondo_brand** `text`
- **color_inverso** `text`
- **logo_master_url** `text`
- **logo_horizontal_light_url** `text`
- **logo_horizontal_dark_url** `text`
- **logo_vertical_url** `text`
- **isotipo_url** `text`
- **favicon_url** `text`
- **header_email_url** `text`
- **footer_doc_url** `text`
- **watermark_url** `text`
- **branding_updated_at** `timestamp with time zone`
- **config_inventario** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **rpi_imss** `text`
- **registro_infonavit** `text`
- **telefono** `text`
- **email_contacto** `text`
- **firmante_poliza** `text`

### `core.gobierno_acta_acuerdos`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **acta_id** `uuid` NOT NULL — FK → `core.gobierno_actas(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **orden** `integer` NOT NULL DEFAULT `1`
- **punto** `text` NOT NULL
- **resultado** `text` NOT NULL DEFAULT `'aprobado'::text`
- **notas** `text`

### `core.gobierno_acta_asistentes`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **acta_id** `uuid` NOT NULL — FK → `core.gobierno_actas(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **socio_id** `uuid` — FK → `core.empresa_socios(id)`
- **presente** `boolean` NOT NULL DEFAULT `true`
- **representado_por** `text`
- **porcentaje** `numeric`

### `core.gobierno_acta_votos`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **acuerdo_id** `uuid` NOT NULL — FK → `core.gobierno_acta_acuerdos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **socio_id** `uuid` — FK → `core.empresa_socios(id)`
- **sentido** `text` NOT NULL
- **representado_por** `text`

### `core.gobierno_actas`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **folio** `text`
- **tipo** `text` NOT NULL
- **fecha** `date` NOT NULL
- **lugar** `text`
- **asunto** `text`
- **quorum_pct** `numeric`
- **orden_dia** `jsonb`
- **protocolizada** `boolean` NOT NULL DEFAULT `false`
- **numero_escritura** `text`
- **notario** `text`
- **fecha_protocolizacion** `date`
- **registro_publico** `text`
- **documento_id** `uuid`
- **estado** `text` NOT NULL DEFAULT `'borrador'::text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **created_by** `uuid` — FK → `core.usuarios(id)`
- **updated_at** `timestamp with time zone`

### `core.gobierno_config`

- **empresa_id** `uuid` NOT NULL — PK, FK → `core.empresas(id)`
- **reglamento_documento_id** `uuid`
- **reglamento_fecha** `date`
- **mandato_meses_default** `integer`
- **consejo_max_miembros** `integer`
- **consejo_sesiones_por_anio** `integer`
- **dividendo_anual_monto** `numeric`
- **dividendo_moneda** `text` NOT NULL DEFAULT `'MXN'::text`
- **tanto_aplica** `boolean` NOT NULL DEFAULT `false`
- **tanto_plazo_dias** `integer`
- **tanto_orden_prelacion** `text`
- **notas** `text`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_by** `uuid` — FK → `core.usuarios(id)`

### `core.gobierno_consejeros`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **organo** `text` NOT NULL DEFAULT `'consejo'::text`
- **socio_id** `uuid` — FK → `core.empresa_socios(id)`
- **persona_id** `uuid`
- **nombre** `text` NOT NULL
- **cargo** `text` NOT NULL DEFAULT `'propietario'::text`
- **ostenta_voto** `boolean` NOT NULL DEFAULT `true`
- **vitalicio** `boolean` NOT NULL DEFAULT `false`
- **periodo_inicio** `date`
- **periodo_fin** `date`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `core.gobierno_mayorias`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tipo_decision** `text` NOT NULL
- **organo** `text` NOT NULL
- **quorum_pct** `numeric`
- **umbral_pct** `numeric` NOT NULL
- **orden** `integer` NOT NULL DEFAULT `1`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `core.modulos`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **slug** `text` NOT NULL
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **seccion** `text` NOT NULL

### `core.notification_definitions`

> Catálogo runtime-editable de emails del sistema. Iniciativa notificaciones-catalogo. Cada handler que envía email lee su slug aquí antes de mandar (Sprint 2 refactoriza los 6 handlers actuales). UNIQUE(slug, empresa_id) permite definición global (empresa_id NULL) + overrides per-empresa con el mismo slug.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **slug** `text` NOT NULL
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **trigger_type** `text` NOT NULL
- **trigger_config** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **from_email** `text` NOT NULL
- **from_name** `text`
- **reply_to** `text`
- **recipients_extra** `jsonb` NOT NULL DEFAULT `'[]'::jsonb`
- **subject_template** `text` NOT NULL
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_by** `uuid` — FK → `core.usuarios(id)`

### `core.notification_log`

> Log append-only de cada envío de email del sistema. Iniciativa notificaciones-catalogo. Sprint 2 conecta los handlers para escribir aquí; Sprint 3 UI expone últimos 20 envíos por definition. Sin TTL en v1 — si en 6 meses crece mucho, agregar policy DELETE WHERE sent_at < now() - interval '90 days'.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **definition_id** `uuid` — FK → `core.notification_definitions(id)`
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **sent_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **status** `text` NOT NULL
- **recipients** `jsonb` NOT NULL
- **subject** `text`
- **resend_id** `text`
- **error_message** `text`
- **triggered_by_user_id** `uuid` — FK → `core.usuarios(id)`
- **context** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`

### `core.permisos_rol`

- **rol_id** `uuid` NOT NULL — PK, FK → `core.roles(id)`
- **modulo_id** `uuid` NOT NULL — PK, FK → `core.modulos(id)`
- **acceso_lectura** `boolean` DEFAULT `true`
- **acceso_escritura** `boolean` DEFAULT `false`

### `core.permisos_usuario_excepcion`

- **usuario_id** `uuid` NOT NULL — PK
- **empresa_id** `uuid` NOT NULL — PK, FK → `core.empresas(id)`
- **modulo_id** `uuid` NOT NULL — PK, FK → `core.modulos(id)`
- **acceso_lectura** `boolean`
- **acceso_escritura** `boolean`

### `core.profiles`

- **id** `uuid` NOT NULL — PK
- **email** `text` NOT NULL DEFAULT `''::text`
- **first_name** `text` DEFAULT `''::text`
- **last_name** `text` DEFAULT `''::text`
- **avatar_url** `text`
- **locale** `text` DEFAULT `'es-MX'::text`
- **is_active** `boolean` DEFAULT `true`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **updated_at** `timestamp with time zone` DEFAULT `now()`

### `core.roles`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **nombre** `text` NOT NULL
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **descripcion** `text`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **puede_aprobar_cierres** `boolean` NOT NULL DEFAULT `false`

### `core.sidebar_oculto`

> Denylist de items top-level del sidebar ocultos globalmente (incluido admin). Presencia de fila = oculto. Solo visibilidad de menú; el acceso real lo gobierna el RBAC. nav_slug = clave de NAV_TO_EMPRESA (no FK: incluye items virtuales como personas_fisicas).

- **nav_slug** `text` NOT NULL — PK
- **oculto_por** `uuid` — FK → `core.usuarios(id)`
- **oculto_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `core.user_presence`

- **user_id** `uuid` NOT NULL — PK
- **email** `text` NOT NULL
- **display_name** `text`
- **avatar_url** `text`
- **current_path** `text` NOT NULL DEFAULT `'/'::text`
- **current_module** `text` NOT NULL DEFAULT `'Overview'::text`
- **last_seen_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **status** `text` NOT NULL DEFAULT `'active'::text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `core.usuarios`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **email** `text` NOT NULL
- **rol** `text` NOT NULL DEFAULT `'viewer'::text`
- **activo** `boolean` DEFAULT `true`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **first_name** `text`
- **welcome_sent_at** `timestamp with time zone`
- **junta_activa_id** `uuid` — FK → `erp.juntas(id)`
- **last_name** `text`

### `core.usuarios_empresas`

- **usuario_id** `uuid` NOT NULL — PK
- **empresa_id** `uuid` NOT NULL — PK, FK → `core.empresas(id)`
- **rol_id** `uuid` — FK → `core.roles(id)`
- **activo** `boolean` DEFAULT `true`

---

## Schema `dilesa`

### `dilesa.activo_casa`

> Satélite 1:1 de dilesa.activos para tipo=casa: vivienda unifamiliar.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **recamaras** `integer`
- **banos** `numeric`
- **m2_construccion** `numeric`
- **m2_terreno** `numeric`
- **niveles** `integer`
- **cochera_autos** `integer`
- **ano_construccion** `integer`
- **estado_conservacion** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_departamento`

> Satélite 1:1 de dilesa.activos para tipo=departamento: unidad habitacional en edificio o complejo.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **recamaras** `integer`
- **banos** `numeric`
- **m2_construccion** `numeric`
- **nivel** `integer`
- **tiene_balcon** `boolean`
- **cajones_estacionamiento** `integer`
- **mantenimiento_mensual** `numeric`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_edificio`

> Satélite 1:1 de dilesa.activos para tipo=edificio.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **niveles** `integer`
- **m2_rentable_total** `numeric`
- **m2_construccion_total** `numeric`
- **elevadores** `integer`
- **uso** `text`
- **cajones_estacionamiento** `integer`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_espectacular`

> Satélite 1:1 de dilesa.activos para tipo=espectacular: panel publicitario sobre estructura.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **caras** `integer`
- **ancho_m** `numeric`
- **alto_m** `numeric`
- **iluminado** `boolean`
- **orientacion** `text`
- **vialidad** `text`
- **trafico_estimado_diario** `integer`
- **anunciante_actual** `text`
- **renta_mensual** `numeric`
- **contrato_vigente_hasta** `date`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_infraestructura`

> Satélite 1:1 de dilesa.activos para tipo=infraestructura: vialidades, canales, áreas verdes, equipamiento.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **subtipo** `text`
- **longitud_m** `numeric`
- **estado_mantenimiento** `text`
- **entregado_a_municipio** `boolean`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_local`

> Satélite 1:1 de dilesa.activos para tipo=local: local comercial.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **m2_rentable** `numeric`
- **frente_m** `numeric`
- **planta** `text`
- **giro_permitido** `text`
- **tiene_bodega** `boolean`
- **banos** `integer`
- **estado_obra** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_lote`

> Satélite 1:1 de dilesa.activos para tipo=lote: manzana, número, condición, dimensiones.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **manzana** `text`
- **numero_lote** `text`
- **condicion** `text`
- **frente_m** `numeric`
- **fondo_m** `numeric`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_nave`

> Satélite 1:1 de dilesa.activos para tipo=nave: nave industrial / bodega.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **m2_techados** `numeric`
- **m2_patio** `numeric`
- **altura_libre_m** `numeric`
- **andenes_carga** `integer`
- **subestacion_electrica** `boolean`
- **uso_suelo_industrial** `boolean`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_plaza`

> Satélite 1:1 de dilesa.activos para tipo=plaza: plaza comercial, activo padre de locales.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **locales_totales** `integer`
- **area_rentable_total_m2** `numeric`
- **area_comun_m2** `numeric`
- **cajones_estacionamiento** `integer`
- **tiene_anchor** `boolean`
- **anchor_nombre** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activo_terreno`

> Satélite 1:1 de dilesa.activos para tipo=terreno: uso de suelo, factibilidades, zonificación.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **uso_suelo** `text`
- **zonificacion** `text`
- **factibilidad_agua** `boolean`
- **factibilidad_drenaje** `boolean`
- **factibilidad_electricidad** `boolean`
- **factibilidad_vialidad** `boolean`
- **areas_afectacion_m2** `numeric`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **tipo_terreno** `text`
- **objetivo** `text`
- **zona_sector** `text`
- **propietario_nombre** `text`
- **propietario_telefono** `text`
- **corredor_nombre** `text`
- **corredor_telefono** `text`
- **precio_solicitado_m2** `numeric`
- **precio_ofertado_m2** `numeric`
- **valor_objetivo_compra** `numeric`
- **origen** `text`
- **estatus_propiedad** `text`
- **etapa** `text`
- **decision_actual** `text`
- **prioridad** `text`
- **responsable** `text`
- **fecha_ultima_revision** `date`
- **siguiente_accion** `text`

### `dilesa.activo_unipolar`

> Satélite 1:1 de dilesa.activos para tipo=unipolar: panel publicitario sobre poste único.

- **activo_id** `uuid` NOT NULL — PK, FK → `dilesa.activos(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **caras** `integer`
- **ancho_m** `numeric`
- **alto_m** `numeric`
- **altura_poste_m** `numeric`
- **iluminado** `boolean`
- **orientacion** `text`
- **vialidad** `text`
- **trafico_estimado_diario** `integer`
- **anunciante_actual** `text`
- **renta_mensual** `numeric`
- **contrato_vigente_hasta** `date`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.activos`

> Portafolio de activos DILESA (master). Discriminador `tipo`; campos específicos por tipo en satélites dilesa.activo_<tipo>. Jerarquía padre/hijo via activo_padre_id. Ver ADR-009/010.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tipo** `text` NOT NULL
- **nombre** `text` NOT NULL
- **estado** `text` NOT NULL DEFAULT `'prospecto'::text`
- **activo_padre_id** `uuid` — FK → `dilesa.activos(id)`
- **clave_interna** `text`
- **municipio** `text`
- **estado_geo** `text`
- **direccion_referencia** `text`
- **latitud** `numeric`
- **longitud** `numeric`
- **area_m2** `numeric`
- **situacion_legal** `text`
- **numero_escritura** `text`
- **clave_catastral** `text`
- **valor_estimado** `numeric`
- **notas** `text`
- **documentos** `jsonb` NOT NULL DEFAULT `'[]'::jsonb`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **modalidad** `text`

### `dilesa.construccion`

> Pivot central del módulo. 1 fila por arranque de construcción = (unidad × prototipo × contratista). Reemplaza "Construcción por Lote" de Coda (47 cols, muchas calculadas). Aquí solo campos físicos; los derivados (días sin avance, efectividad, etc.) son vistas SQL. El avance_pct es pre-calculado por trigger desde tareas_terminadas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text` NOT NULL
- **unidad_id** `uuid` NOT NULL — FK → `dilesa.unidades(id)`
- **producto_id** `uuid` NOT NULL — FK → `dilesa.productos(id)`
- **contratista_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **supervisor_persona_id** `uuid` — FK → `erp.personas(id)`
- **fecha_arranque** `date`
- **fecha_compromiso_terminar** `date`
- **fecha_terminada** `date`
- **fecha_seguro_calidad** `date`
- **fecha_extraccion** `date`
- **fecha_paquete_ruv** `date`
- **fecha_dtu** `date`
- **cuv** `text`
- **frente_ruv** `text`
- **avance_pct** `numeric` NOT NULL DEFAULT `0`
- **mo_ejecutado** `numeric` NOT NULL DEFAULT `0`
- **m2_construccion** `numeric`
- **precio_mo_x_m2** `numeric`
- **valor_contrato_mo** `numeric`
- **estado** `text` NOT NULL DEFAULT `'arrancada'::text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`
- **costo_materiales** `numeric`
- **frente_id** `uuid` — FK → `dilesa.ruv_frentes(id)`

### `dilesa.construccion_tareas_terminadas`

> Log append-only de tareas terminadas. Append una fila por cada tarea que el supervisor cierra. El trigger tg_construccion_avance recalcula avance_pct en la construccion correspondiente y dispara "20% → disponible" cuando aplique.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **construccion_id** `uuid` NOT NULL — FK → `dilesa.construccion(id)`
- **plantilla_tarea_id** `uuid` NOT NULL — FK → `dilesa.plantilla_tareas(id)`
- **fecha_terminada** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **tiempo_real_dias** `numeric`
- **mano_obra_pagada** `numeric`
- **revisado_por_persona_id** `uuid` — FK → `erp.personas(id)`
- **fecha_pagada** `date`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`
- **revisado_por_user_id** `uuid` — FK → `core.usuarios(id)`

### `dilesa.contratistas_datos`

> Satélite 1:1 con erp.personas (donde tipo=contratista). Datos específicos DILESA: REPSE, retención, abreviación interna, etc. KPIs (efectividad, días sin avance, etc.) son derivados vía vistas, no se almacenan aquí.

- **persona_id** `uuid` NOT NULL — PK, FK → `erp.personas(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **abreviacion** `text`
- **persona_fisica_o_moral** `text`
- **representante_legal** `text`
- **repse** `text`
- **registro_patronal** `text`
- **retencion_pct** `numeric` DEFAULT `0`
- **domicilio** `text`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.contrato_lotes`

> N:M entre contratos_construccion y construccion. Reemplaza la columna CSV "ID Construcción" del Coda. Una construcción puede tener varios contratos (ej. obra extra) y un contrato puede cubrir varios lotes.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **contrato_id** `uuid` NOT NULL — FK → `dilesa.contratos_construccion(id)`
- **construccion_id** `uuid` NOT NULL — FK → `dilesa.construccion(id)`
- **monto_lote** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.contratos_construccion`

> Cabecera de contrato con un contratista. 1 contrato puede cubrir N lotes (ver tabla N:M contrato_lotes). En Coda esto es la columna CSV "ID Construcción"; en BSOP es JOIN limpio.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text` NOT NULL
- **fecha_contrato** `date` NOT NULL
- **contratista_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **proyecto_id** `uuid` — FK → `dilesa.proyectos(id)`
- **valor_total** `numeric` NOT NULL DEFAULT `0`
- **fianzas_url** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`
- **tipo** `text` NOT NULL DEFAULT `'vivienda'::text`
- **anticipo_pct** `numeric` NOT NULL DEFAULT `0`
- **retencion_pct** `numeric` NOT NULL DEFAULT `5`
- **valor_subtotal** `numeric`
- **valor_iva** `numeric`
- **iva_tasa** `numeric`
- **partida_id** `uuid` — FK → `erp.presupuesto_partidas(id)`
- **cotizacion_id** `uuid` — FK → `erp.cotizaciones(id)`
- **objeto** `text`
- **fecha_inicio** `date`
- **fecha_fin** `date`
- **fianza_pct** `numeric`
- **periodicidad_estimaciones_dias** `integer`
- **creado_por** `uuid`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `dilesa.estimacion_tareas`

> Vínculo M:1 estimación ← tareas terminadas. UNIQUE absoluto en tarea_terminada_id impide que una tarea entre a 2 estimaciones. Si una estimación se cancela, el DELETE CASCADE libera las tareas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **estimacion_id** `uuid` NOT NULL — FK → `dilesa.estimaciones(id)`
- **tarea_terminada_id** `uuid` NOT NULL — FK → `dilesa.construccion_tareas_terminadas(id)`
- **construccion_id** `uuid` NOT NULL — FK → `dilesa.construccion(id)`
- **monto_calculado** `numeric` NOT NULL
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.estimaciones`

> Ciclo de pago semanal a contratistas DILESA. ADR-033. Una fila por cierre (típicamente miércoles) por contratista. Acumula tareas palomeadas no pagadas vía dilesa.estimacion_tareas (M:1).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text` NOT NULL
- **contratista_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **fecha_cierre** `date` NOT NULL
- **fecha_pago_programado** `date` NOT NULL
- **monto_bruto** `numeric` NOT NULL DEFAULT `0`
- **retencion_pct** `numeric` NOT NULL DEFAULT `5.0`
- **retencion_monto** `numeric` NOT NULL DEFAULT `0`
- **monto_neto** `numeric` NOT NULL DEFAULT `0`
- **factura_url** `text`
- **factura_folio** `text`
- **factura_fecha** `date`
- **aprobada_por_user_id** `uuid` — FK → `core.usuarios(id)`
- **aprobada_at** `timestamp with time zone`
- **pagada_por_user_id** `uuid` — FK → `core.usuarios(id)`
- **pagada_at** `timestamp with time zone`
- **referencia_pago** `text`
- **estado** `text` NOT NULL DEFAULT `'borrador'::text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.etapas_construccion`

> Catálogo de etapas del proceso de construcción (PRELIMINARES, CIMENTACION, ALBAÑILERIA, etc.). El orden define la secuencia visual; el avance no se calcula por etapa sino por tareas terminadas (ADR-032 D3).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **orden** `integer` NOT NULL
- **dias_estimados** `integer` DEFAULT `0`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.obra_estimaciones`

> Estimaciones de monto por contrato de obra no-vivienda (anticipo + numeradas + finiquito; etiqueta libre). IVA (8 frontera / 16 excepcion) desglosado donde este especificado. Vivienda usa dilesa.estimaciones (por tareas terminadas, ADR-033).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **contrato_id** `uuid` NOT NULL — FK → `dilesa.contratos_construccion(id)`
- **etiqueta** `text` NOT NULL
- **orden** `integer` NOT NULL DEFAULT `0`
- **fecha** `date`
- **factura_ref** `text`
- **subtotal** `numeric`
- **iva** `numeric`
- **iva_tasa** `numeric`
- **monto_total** `numeric` NOT NULL DEFAULT `0`
- **retencion** `numeric` NOT NULL DEFAULT `0`
- **es_anticipo** `boolean` NOT NULL DEFAULT `false`
- **es_finiquito** `boolean` NOT NULL DEFAULT `false`
- **nota_pago** `text`
- **source_ref** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **creado_por** `uuid`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `dilesa.obra_presupuesto_deprecated`

> DEPRECATED 2026-06-05 — superseded por erp.presupuesto_partidas (ADR-040). No usar; conservada como referencia histórica del traspaso de obra (iniciativa dilesa-compras).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **etapa** `text`
- **concepto** `text` NOT NULL
- **orden** `integer` NOT NULL DEFAULT `0`
- **presupuesto_previo** `numeric`
- **presupuesto_actualizado** `numeric`
- **gasto_real_subtotal** `numeric`
- **gasto_real_iva** `numeric`
- **gasto_real_total** `numeric`
- **gasto_real_iva_tasa** `numeric`
- **proveedor_texto** `text`
- **proveedor_persona_id** `uuid` — FK → `erp.personas(id)`
- **contrato_id** `uuid` — FK → `dilesa.contratos_construccion(id)`
- **factura_ref** `text`
- **fecha_compromiso** `date`
- **notas** `text`
- **source_ref** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.plantilla_proyecto_tareas`

> Catálogo canónico de tareas para proyectos DILESA (anteproyecto, desarrollo o ambas). 35 tareas seed importadas de Coda + gate + cotizaciones. Sprint 3 de dilesa-proyectos-anteproyectos.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **aplicacion** `text` NOT NULL
- **tipo** `text` NOT NULL
- **subtipo** `text`
- **duracion_dias_habiles** `integer` NOT NULL
- **orden_default** `integer` NOT NULL DEFAULT `0`
- **entidad_responsable** `text` NOT NULL
- **obligatoriedad** `text` NOT NULL
- **se_entrega_a** `text`
- **requiere_archivo** `boolean` NOT NULL DEFAULT `false`
- **formato_archivo** `text`
- **activa** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.plantilla_proyecto_tareas_dependencias`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **plantilla_tarea_id** `uuid` NOT NULL — FK → `dilesa.plantilla_proyecto_tareas(id)`
- **depende_de_plantilla_tarea_id** `uuid` NOT NULL — FK → `dilesa.plantilla_proyecto_tareas(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.plantilla_tareas`

> Receta de cada prototipo: qué tareas, en qué etapa, con qué % de costo y tiempo estimado. Suma de porcentaje_costo por producto debe ser ~100%. Fuente para el cálculo de avance (ADR-032 D3).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **producto_id** `uuid` NOT NULL — FK → `dilesa.productos(id)`
- **tarea_id** `uuid` NOT NULL — FK → `dilesa.tareas_construccion(id)`
- **etapa_id** `uuid` NOT NULL — FK → `dilesa.etapas_construccion(id)`
- **porcentaje_costo** `numeric` NOT NULL DEFAULT `0`
- **costo_mo_plantilla** `numeric` NOT NULL DEFAULT `0`
- **tiempo_dias** `numeric` NOT NULL DEFAULT `0`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.productos`

> Catálogo de unidad-tipo por proyecto (prototipo de vivienda, tipo de local, etc.). Polimórfico via atributos jsonb. Ver ADR-009.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **atributos** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **valor_comercial_referencia** `numeric`
- **costo_referencia** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **planos** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **costo_urbanizacion_referencia** `numeric`
- **costo_materiales_referencia** `numeric`
- **costo_mo_referencia** `numeric`
- **registro_ruv_referencia** `numeric`
- **seguro_calidad_referencia** `numeric`
- **costo_comercializacion_referencia** `numeric`

### `dilesa.promociones`

> Catálogo de promociones/bonos DILESA. Sprint 7a Ventas. Vendedor selecciona al capturar Solicitud si aplica al prototipo de la unidad.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **productos_aplicables** `ARRAY` NOT NULL DEFAULT `'{}'::uuid[]`
- **activa** `boolean` NOT NULL DEFAULT `true`
- **vigencia_inicio** `date`
- **vigencia_fin** `date`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.proyecto_activos`

> Vínculo M:N Proyecto↔Activo. rol=input: activos sobre los que interviene; rol=output: activos que genera. Ver ADR-010.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **activo_id** `uuid` NOT NULL — FK → `dilesa.activos(id)`
- **rol** `text` NOT NULL
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.proyecto_documentos`

> Legajo documental de un proyecto (planos, factibilidades, permisos, contratos).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **nombre** `text` NOT NULL
- **tipo** `text`
- **url** `text` NOT NULL
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **created_by** `uuid` — FK → `core.usuarios(id)`
- **deleted_at** `timestamp with time zone`

### `dilesa.proyecto_hitos`

> Hitos macro de un proyecto (factibilidades, permisos, obra, comercialización, cierre).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **fecha_objetivo** `date`
- **fecha_real** `date`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **orden** `integer` NOT NULL DEFAULT `0`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.proyecto_planos`

> Sprint 4D. Versiones del plano del anteproyecto (iteraciones). El archivo físico vive en erp.adjuntos con entidad_tipo=proyecto_plano. Solo 1 vigente=true por proyecto, enforcing via índice unique parcial. ai_analisis jsonb reservado para Sprint 4E (Claude Vision).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **version** `integer` NOT NULL
- **descripcion** `text`
- **vigente** `boolean` NOT NULL DEFAULT `false`
- **ai_analisis** `jsonb`
- **subido_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.proyecto_presupuesto_partidas`

> Partidas de presupuesto por proyecto (anteproyecto preliminar → desarrollo aprobado → ejecución). Estado discriminator preserva la trayectoria inline. Sprint 3 de dilesa-proyectos-anteproyectos.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **tarea_origen_id** `uuid` — FK → `dilesa.proyecto_tareas(id)`
- **partida** `text` NOT NULL
- **descripcion** `text`
- **unidad** `text`
- **cantidad** `numeric`
- **monto_estimado** `numeric`
- **monto_aprobado** `numeric`
- **monto_ejercido** `numeric` NOT NULL DEFAULT `0`
- **fuente** `text`
- **proveedor_persona_id** `uuid` — FK → `erp.personas(id)`
- **estado** `text` NOT NULL DEFAULT `'preliminar'::text`
- **autorizado_at** `timestamp with time zone`
- **autorizado_por** `uuid`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.proyecto_prorrateo`

> Asignación explícita de CapEx compartido a sub-proyectos. Solo aplica con regla_prorrateo=manual; las otras reglas se calculan en vista. Ver ADR-010.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_madre_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **sub_proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **porcentaje** `numeric` NOT NULL
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.proyecto_responsables`

> Responsables de un proyecto. Internos via empleado_id; externos (arquitecto, notario, contratista) via nombre_externo.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **empleado_id** `uuid` — FK → `erp.empleados(id)`
- **rol** `text` NOT NULL
- **externo** `boolean` NOT NULL DEFAULT `false`
- **nombre_externo** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.proyecto_tarea_pasos`

> Pasos del ciclo de vida operativo de una tarea (cotización, factura, pago, resultado). Cada paso captura monto + documento + fecha + estado. UNIQUE(tarea_id, paso) garantiza un solo row por paso. Sprint 3 de dilesa-proyectos-checklist-inline.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tarea_id** `uuid` NOT NULL — FK → `dilesa.proyecto_tareas(id)`
- **paso** `text` NOT NULL
- **monto** `numeric`
- **documento_url** `text`
- **fecha** `date`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **autorizado_at** `timestamp with time zone`
- **autorizado_por** `uuid` — FK → `core.usuarios(id)`

### `dilesa.proyecto_tareas`

> Tareas de un proyecto. Agnóstico al tipo de proyecto (hereda el patrón del módulo Tareas existente).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **titulo** `text` NOT NULL
- **descripcion** `text`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **prioridad** `text` NOT NULL DEFAULT `'media'::text`
- **responsable_id** `uuid` — FK → `erp.empleados(id)`
- **fecha_limite** `date`
- **fecha_completada** `date`
- **orden** `integer` NOT NULL DEFAULT `0`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **plantilla_tarea_id** `uuid` — FK → `dilesa.plantilla_proyecto_tareas(id)`
- **tipo_snapshot** `text`
- **subtipo_snapshot** `text`
- **entidad_responsable_snapshot** `text`
- **aplicacion_snapshot** `text`
- **obligatoriedad_snapshot** `text`
- **se_entrega_a_snapshot** `text`
- **requiere_archivo_snapshot** `boolean`
- **formato_archivo_snapshot** `text`
- **duracion_dias_habiles_snapshot** `integer`
- **fecha_objetivo_inicio** `date`
- **fecha_objetivo_fin** `date`
- **resultado_monto** `numeric`
- **resultado_documento_url** `text`

### `dilesa.proyecto_tareas_dependencias`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **tarea_id** `uuid` NOT NULL — FK → `dilesa.proyecto_tareas(id)`
- **depende_de_tarea_id** `uuid` NOT NULL — FK → `dilesa.proyecto_tareas(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.proyectos`

> Proyectos DILESA (master). Discriminador `tipo` (anteproyecto/desarrollo/etc.). Jerarquía madre/sub-proyecto via proyecto_padre_id; anteproyecto ganador via proyecto_predecesor_id. Ver ADR-009/010.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tipo** `text` NOT NULL
- **nombre** `text` NOT NULL
- **estado** `text` NOT NULL DEFAULT `'propuesta'::text`
- **proyecto_padre_id** `uuid` — FK → `dilesa.proyectos(id)`
- **proyecto_predecesor_id** `uuid` — FK → `dilesa.proyectos(id)`
- **plantilla_id** `uuid` — FK → `dilesa.proyectos_plantillas(id)`
- **regla_prorrateo** `text` NOT NULL DEFAULT `'m2_beneficiados'::text`
- **presupuesto_estimado** `numeric`
- **fecha_inicio** `date`
- **fecha_fin_estimada** `date`
- **notas** `text`
- **documentos** `jsonb` NOT NULL DEFAULT `'[]'::jsonb`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **area_m2** `numeric`
- **area_vendible_m2** `numeric`
- **areas_verdes_m2** `numeric`
- **lotes_proyectados** `integer`
- **fecha_licencia** `date`
- **costo_terreno** `numeric`
- **costo_urbanizacion** `numeric`
- **costo_construccion** `numeric`
- **costo_comercializacion** `numeric`
- **clave_interna** `text`
- **precio_m2_excedente** `numeric`
- **tamano_lote_promedio** `numeric`
- **clasificacion_inmobiliaria** `text`
- **plano_oficial_url** `text`
- **image_url** `text`
- **acreditacion_escritura** `text`
- **objetivo_trimestral** `integer`
- **area_comercial_m2** `numeric`
- **area_residencial_m2** `numeric`
- **area_vialidades_m2** `numeric`
- **costo_mo** `numeric`
- **valor_comercial_referencia** `numeric`
- **valor_comercial_proyecto** `numeric`
- **costo_urbanizacion_referencia** `numeric`
- **costo_materiales_referencia** `numeric`
- **costo_materiales_proyecto** `numeric`
- **costo_mo_referencia** `numeric`
- **registro_ruv_referencia** `numeric`
- **registro_ruv_proyecto** `numeric`
- **seguro_calidad_referencia** `numeric`
- **seguro_calidad_proyecto** `numeric`
- **costo_comercializacion_referencia** `numeric`
- **infraestructura_cabecera_necesaria** `boolean` NOT NULL DEFAULT `false`
- **valor_predio** `numeric`
- **prototipos_referencia** `ARRAY` NOT NULL DEFAULT `'{}'::text[]`
- **clasificaciones_inmobiliarias** `ARRAY` NOT NULL DEFAULT `'{}'::text[]`
- **prototipo_referencia_id** `uuid` — FK → `dilesa.productos(id)`

### `dilesa.proyectos_plantillas`

> Plantillas editables por tipo de proyecto. definicion jsonb = tareas/hitos/KPIs. es_oficial=true cuando la plantilla maduró. empresa_id NULL = plantilla global.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` — FK → `core.empresas(id)`
- **tipo_proyecto** `text` NOT NULL
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **es_oficial** `boolean` NOT NULL DEFAULT `false`
- **definicion** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.ruv_documentos_catalogo`

> Catálogo de tipos de documento requeridos para el paquete RUV (27 tipos en Coda). Fuente: tabla "Documentos Necesarios" de Coda.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **orden** `integer`
- **descripcion** `text`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `dilesa.ruv_frente_documentos`

> Estado (cargado/pendiente) de cada documento del catálogo por frente RUV. archivo_url reservado para adjuntos vía Storage (futuro).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **frente_id** `uuid` NOT NULL — FK → `dilesa.ruv_frentes(id)`
- **documento_catalogo_id** `uuid` NOT NULL — FK → `dilesa.ruv_documentos_catalogo(id)`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **fecha_carga** `date`
- **archivo_url** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.ruv_frentes`

> Oferta de viviendas ante INFONAVIT (RUV). Fuente: tabla "Frente RUV" de Coda. Agrupa viviendas (dilesa.construccion.frente_id) para el trámite de registro de paquete.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` — FK → `dilesa.proyectos(id)`
- **nombre** `text` NOT NULL
- **id_oferta** `bigint`
- **id_orden** `bigint`
- **fecha_inicio** `date`
- **fecha_fin** `date`
- **viviendas_oferta** `integer`
- **coda_id** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.tareas_construccion`

> Diccionario de tareas posibles (~500 entradas migradas desde Coda). Cada plantilla de prototipo selecciona un subset y asigna porcentaje de costo, tiempo estimado y etapa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.tipos_credito`

> Catálogo de tipos de crédito DILESA. Sprint 7a Ventas. costo_venta_adicional_pct se suma al precio (ej. Fovissste +6%); apoyo_infonavit_monto se resta del pago directo del cliente (ej. $30,000 en Infonavit Tradicional).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **costo_venta_adicional_pct** `numeric` NOT NULL DEFAULT `0`
- **apoyo_infonavit_monto** `numeric` NOT NULL DEFAULT `0`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.unidades`

> Pieza física vendible/rentable (lote, local, departamento). producto_id reclasificable hasta comprometer; activo_id se llena cuando la unidad se libera al portafolio. Ver ADR-010.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `dilesa.proyectos(id)`
- **producto_id** `uuid` — FK → `dilesa.productos(id)`
- **activo_id** `uuid` — FK → `dilesa.activos(id)`
- **identificador** `text` NOT NULL
- **estado** `text` NOT NULL DEFAULT `'planeada'::text`
- **area_m2** `numeric`
- **precio** `numeric`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **manzana** `text`
- **numero_lote** `text`
- **calle** `text`
- **numero_oficial** `text`
- **tipo_lote** `text`
- **es_esquina** `boolean`
- **tiene_frente_verde** `boolean`
- **m2_construccion** `numeric`
- **valor_venta_futuro_snapshot** `numeric` DEFAULT `0`
- **es_muestra** `boolean` NOT NULL DEFAULT `false`
- **frente_id** `uuid` — FK → `dilesa.ruv_frentes(id)`

### `dilesa.v_construccion_tareas_terminadas_con_mo` _(view)_

> Tareas terminadas con MO calculado = valor_contrato_mo × porcentaje_costo/100. Si mano_obra_pagada (override manual o legado de Coda) está poblado, lo respeta. ADR-032 D3 — MO por tarea es derivado, no se captura en UI.

- **id** `uuid`
- **empresa_id** `uuid`
- **construccion_id** `uuid`
- **plantilla_tarea_id** `uuid`
- **fecha_terminada** `date`
- **tiempo_real_dias** `numeric`
- **revisado_por_persona_id** `uuid`
- **fecha_pagada** `date`
- **notas** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **mo_calculado** `numeric`

### `dilesa.v_contratista_obra` _(view)_

- **empresa_id** `uuid`
- **contratista_id** `uuid`
- **contratista** `text`
- **viviendas** `bigint`
- **mo_contratado** `numeric`
- **mo_ejecutado** `numeric`
- **pct_ejecutado** `numeric`
- **avance_real** `numeric`
- **avance_esperado** `numeric`
- **efectividad_pct** `numeric`
- **vencidas** `bigint`

### `dilesa.v_estimaciones_resumen` _(view)_

> Agregaciones de estimaciones por contratista × estado × semana ISO. Para dashboards de pagos.

- **empresa_id** `uuid`
- **contratista_id** `uuid`
- **estado** `text`
- **anio_iso** `integer`
- **semana_iso** `integer`
- **estimaciones_count** `bigint`
- **monto_bruto_total** `numeric`
- **retencion_total** `numeric`
- **monto_neto_total** `numeric`

### `dilesa.v_inventario_prototipo` _(view)_

- **prototipo_id** `uuid`
- **empresa_id** `uuid`
- **inventario_construccion** `bigint`
- **inventario_terminado** `bigint`
- **inventario_asignado** `bigint`
- **en_inventario** `bigint`
- **inventario_disponible** `bigint`

### `dilesa.v_margen_prototipo` _(view)_

- **prototipo_id** `uuid`
- **empresa_id** `uuid`
- **proyecto_id** `uuid`
- **nombre** `text`
- **valor_comercial** `numeric`
- **costo_terreno** `numeric`
- **costo_urbanizacion** `numeric`
- **costo_materiales** `numeric`
- **costo_mo** `numeric`
- **registro_ruv** `numeric`
- **seguro_calidad** `numeric`
- **costo_comercializacion** `numeric`
- **costo_total** `numeric`
- **utilidad** `numeric`
- **margen_pct** `numeric`

### `dilesa.v_proyecto_avance` _(view)_

> Avance del proyecto derivado de `proyecto_tarea_pasos`. Promedio ponderado por obligatoriedad de la tarea (obligatoria=1, condicional=0.5, opcional=0). Sprint 3 de dilesa-proyectos-checklist-inline.

- **proyecto_id** `uuid`
- **empresa_id** `uuid`
- **tareas_aplicables** `bigint`
- **tareas_completadas** `bigint`
- **avance_pct** `numeric`

### `dilesa.v_proyecto_avances` _(view)_

> Agregados derivados de `dilesa.unidades` por proyecto. Sprint A: avance %, conteos básicos, ticket, ventas, estado sugerido. Sprint C: segmentación comercial/residencial, lote promedio, inventario formalizado/disponible (excluye muestra), casas asignadas/entregadas/muestra, densidad de vivienda. Reemplaza las 46 fórmulas de la tabla Coda *Proyectos.

- **proyecto_id** `uuid`
- **empresa_id** `uuid`
- **lotes_total** `bigint`
- **lotes_construidos** `bigint`
- **lotes_vendidos** `bigint`
- **lotes_urbanizados** `bigint`
- **casas_terminadas** `bigint`
- **casas_en_construccion** `bigint`
- **casas_escrituradas** `bigint`
- **avance_urb_pct** `numeric`
- **avance_const_pct** `numeric`
- **avance_vts_pct** `numeric`
- **parque_disponible** `bigint`
- **ticket_promedio** `numeric`
- **ventas_totales** `numeric`
- **estado_sugerido** `text`
- **estado_actual** `text`
- **tipo** `text`
- **casas_asignadas** `bigint`
- **casas_entregadas** `bigint`
- **casas_muestra** `bigint`
- **inventario_formalizado** `bigint`
- **inventario_disponible_venta** `bigint`
- **lotes_comerciales** `bigint`
- **lotes_residenciales** `bigint`
- **tamano_lote_promedio_m2** `numeric`
- **densidad_vivienda** `numeric`

### `dilesa.v_ruv_frente_avance` _(view)_

> Avance del trámite RUV por frente: lotes ligados, viviendas en construcción, CUVs, DTU, seguro de calidad, paquete RUV y documentos pendientes. Deriva de dilesa.unidades (frente_id) ⋈ construccion + ruv_frente_documentos.

- **frente_id** `uuid`
- **empresa_id** `uuid`
- **proyecto_id** `uuid`
- **nombre** `text`
- **viviendas_oferta** `integer`
- **lotes** `bigint`
- **viviendas** `bigint`
- **cuvs_emitidos** `bigint`
- **con_dtu** `bigint`
- **con_seguro_calidad** `bigint`
- **con_paquete_ruv** `bigint`
- **documentos_pendientes** `bigint`
- **pct_paquete_ruv** `numeric`

### `dilesa.v_tareas_pendientes_de_pago` _(view)_

> Tareas palomeadas listas para entrar a próxima estimación. ADR-033 D10. Excluye obras canceladas y tareas ya vinculadas a alguna estimación.

- **tarea_terminada_id** `uuid`
- **empresa_id** `uuid`
- **construccion_id** `uuid`
- **construccion_codigo** `text`
- **unidad_id** `uuid`
- **contratista_id** `uuid`
- **plantilla_tarea_id** `uuid`
- **fecha_terminada** `date`
- **revisado_por_user_id** `uuid`
- **revisado_por_persona_id** `uuid`
- **monto_calculado** `numeric`

### `dilesa.v_unidad_hold_queue` _(view)_

> Cola de holds por unidad — la posición 1 es el líder con hold activo. Solo aplica a ventas creadas en BSOP (coda_row_id IS NULL), Fase 1 (Solicitud), estado activa.

- **unidad_id** `uuid`
- **venta_id** `uuid`
- **empresa_id** `uuid`
- **persona_id** `uuid`
- **vendedor_usuario_id** `uuid`
- **created_at** `timestamp with time zone`
- **expira_at** `timestamp with time zone`
- **notif_hold_creado_at** `timestamp with time zone`
- **notif_hold_promovido_at** `timestamp with time zone`
- **notif_hold_4h_at** `timestamp with time zone`
- **posicion** `bigint`

### `dilesa.venta_fase_catalogo`

> Catálogo de las 17 fases del pipeline de ventas DILESA (Coda "Fase de Venta"). Ver mapeo § 6.3.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **posicion** `integer` NOT NULL
- **nombre** `text` NOT NULL
- **rol** `text`
- **descripcion** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.venta_fases`

> Log del pipeline de una venta: una fila por fase alcanzada con fecha y quién la registró. Timeline del pipeline. Ver mapeo § 6.3.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **venta_id** `uuid` NOT NULL — FK → `dilesa.ventas(id)`
- **fase** `text` NOT NULL
- **posicion** `integer`
- **fecha** `date`
- **registrado_por** `uuid` — FK → `core.usuarios(id)`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `dilesa.venta_pagos`

> Depósitos/pagos de un cliente sobre su venta (1:N). Coda "Depositos Clientes". Ver mapeo § 6.4.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **venta_id** `uuid` NOT NULL — FK → `dilesa.ventas(id)`
- **fecha** `date`
- **monto** `numeric` NOT NULL
- **tipo** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`

### `dilesa.ventas`

> Venta de una unidad a un comprador. persona_id → erp.personas (cross-schema). El pipeline de fases vive en dilesa.venta_fases. Ver mapeo § 6.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **unidad_id** `uuid` — FK → `dilesa.unidades(id)`
- **estado** `text` NOT NULL DEFAULT `'activa'::text`
- **fase_actual** `text`
- **fase_posicion** `integer`
- **tipo_credito** `text`
- **valor_comercial** `numeric`
- **valor_escrituracion** `numeric`
- **precio_asignacion** `numeric`
- **monto_credito_titular** `numeric`
- **monto_credito_cotitular** `numeric`
- **credito_titular_ref** `text`
- **credito_cotitular_ref** `text`
- **enganche_requerido** `numeric`
- **descuento_total** `numeric`
- **comision_vendedor** `numeric`
- **comision_gerencia** `numeric`
- **anticipo_comision** `numeric`
- **vendedor** `text`
- **notario** `text`
- **casa_valuadora** `text`
- **monto_avaluo** `numeric`
- **gastos_escrituracion** `numeric`
- **numero_escritura** `text`
- **fecha_escritura** `date`
- **es_pep** `boolean`
- **ocupacion** `text`
- **ine_numero** `text`
- **forma_pago** `text`
- **uso_efectivo** `text`
- **conocimiento_dueno_beneficiario** `text`
- **motivo_desasignacion** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`
- **vendedor_usuario_id** `uuid` — FK → `core.usuarios(id)`
- **productos_adicionales** `numeric` NOT NULL DEFAULT `0`
- **expira_at** `timestamp with time zone`
- **expirada_at** `timestamp with time zone`
- **notif_hold_creado_at** `timestamp with time zone`
- **notif_hold_promovido_at** `timestamp with time zone`
- **notif_hold_4h_at** `timestamp with time zone`
- **notif_hold_expirada_at** `timestamp with time zone`
- **enganche_num_parcialidades** `integer` NOT NULL DEFAULT `1`
- **enganche_fecha_primer_pago** `date`
- **enganche_periodicidad** `text` NOT NULL DEFAULT `'mensual'::text`
- **valuador_id** `uuid` — FK → `erp.personas(id)`
- **fecha_solicitud_avaluo** `date`
- **fecha_avaluo_cerrado** `date`
- **notif_solicitud_avaluo_at** `timestamp with time zone`
- **notario_id** `uuid` — FK → `erp.personas(id)`
- **fecha_solicitud_dictamen** `date`
- **fecha_dictaminada** `date`
- **notif_solicitud_dictamen_at** `timestamp with time zone`
- **fecha_validacion_patronal** `date`
- **fecha_firma_programada** `date`
- **hora_firma_programada** `time without time zone`
- **monto_credito_directo** `numeric`
- **cd_plan_pagos** `jsonb` NOT NULL DEFAULT `'[]'::jsonb`
- **cd_tiie28_pct** `numeric`
- **cd_spread_moratorio_pct** `numeric` DEFAULT `4`
- **cd_interes_ordinario_pct** `numeric` DEFAULT `0`
- **cd_fecha_suscripcion** `date`
- **cd_aval_nombre** `text`
- **cd_aval_domicilio** `text`
- **numero_cheque_notaria** `text`
- **monto_cheque_notaria** `numeric`
- **fecha_detonacion** `date`
- **monto_detonado** `numeric`

---

## Schema `erp`

### `erp.activos`

> Registro de activos fijos por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **clasificacion** `text`
- **tipo** `text`
- **marca** `text`
- **modelo** `text`
- **numero_serie** `text`
- **fecha_adquisicion** `date`
- **costo_adquisicion** `numeric`
- **valor_actual** `numeric`
- **vida_util_anios** `integer`
- **ubicacion** `text`
- **responsable_id** `uuid` — FK → `erp.empleados(id)`
- **estado_id** `uuid`
- **metadata** `jsonb`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`

### `erp.activos_mantenimiento`

> Historial de mantenimientos preventivos y correctivos de activos.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **activo_id** `uuid` NOT NULL — FK → `erp.activos(id)`
- **tipo** `text`
- **descripcion** `text`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **costo** `numeric`
- **proveedor_id** `uuid` — FK → `erp.proveedores(id)`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.adjuntos`

> Archivos adjuntos polimórficos por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **entidad_tipo** `text` NOT NULL
- **entidad_id** `uuid` NOT NULL
- **uploaded_by** `uuid` — FK → `core.usuarios(id)`
- **nombre** `text` NOT NULL
- **url** `text` NOT NULL
- **tipo_mime** `text`
- **tamano_bytes** `bigint`
- **metadata** `jsonb`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **rol** `text` NOT NULL DEFAULT `'anexo'::text`

### `erp.almacenes`

> Ubicaciones físicas de inventario por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **ubicacion** `text`
- **responsable_id** `uuid` — FK → `erp.empleados(id)`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.aprobaciones`

> Flujo de aprobaciones multi-paso por entidad y empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **entidad_tipo** `text` NOT NULL
- **entidad_id** `uuid` NOT NULL
- **paso** `integer` NOT NULL DEFAULT `1`
- **aprobador_id** `uuid` — FK → `core.usuarios(id)`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **comentario** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cajas`

> Cajas registradoras por empresa (POS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **ubicacion** `text`
- **responsable_id** `uuid` — FK → `erp.empleados(id)`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.categorias_producto`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **color** `text`
- **orden** `integer` NOT NULL DEFAULT `0`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.citas`

> Citas con clientes: servicio ANSA, visitas DILESA, ventas. Vinculada a persona o cliente.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tipo** `text` NOT NULL
- **persona_id** `uuid` — FK → `erp.personas(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **responsable_id** `uuid` — FK → `erp.empleados(id)`
- **fecha_hora** `timestamp with time zone` NOT NULL
- **duracion_minutos** `integer` DEFAULT `60`
- **lugar** `text`
- **estado** `text` NOT NULL DEFAULT `'programada'::text`
- **notas** `text`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.clientes`

> Catálogo de clientes vinculados a erp.personas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **tipo** `text`
- **perfil_extra** `jsonb`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`

### `erp.cobranza`

> Programa de pagos de una venta inmobiliaria.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **venta_id** `uuid` NOT NULL — FK → `erp.ventas_inmobiliarias(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **numero_pago** `integer` NOT NULL
- **monto** `numeric` NOT NULL
- **fecha_vencimiento** `date` NOT NULL
- **fecha_pago** `date`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.conceptos_compra`

> Catálogo jerárquico de conceptos de compra de obra (etapa→capitulo→concepto). tipo_insumo no vive aquí (es atributo de la partida/línea). ADR-040.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **padre_id** `uuid` — FK → `erp.conceptos_compra(id)`
- **nivel** `text` NOT NULL
- **codigo** `text` NOT NULL
- **nombre** `text` NOT NULL
- **orden** `integer` NOT NULL DEFAULT `0`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `erp.conciliaciones`

> Conciliación entre movimientos bancarios y gastos.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **movimiento_bancario_id** `uuid` NOT NULL — FK → `erp.movimientos_bancarios(id)`
- **gasto_id** `uuid` — FK → `erp.gastos(id)`
- **monto_aplicado** `numeric` NOT NULL
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.contratos`

> Contratos inmobiliarios ligados a una venta.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **venta_id** `uuid` — FK → `erp.ventas_inmobiliarias(id)`
- **tipo** `text`
- **numero_contrato** `text`
- **contenido_url** `text`
- **fecha_firma** `date`
- **fecha_vencimiento** `date`
- **estado_id** `uuid`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.corte_conteo_denominaciones`

> Conteo físico de billetes y monedas al cierre de caja.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **corte_id** `uuid` NOT NULL — FK → `erp.cortes_caja(id)`
- **denominacion** `numeric` NOT NULL
- **tipo** `text` NOT NULL
- **cantidad** `integer` NOT NULL DEFAULT `0`
- **subtotal** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cortes_caja`

> Cortes de caja por empresa con referencias a turno y cajero.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **turno_id** `uuid` — FK → `erp.turnos(id)`
- **cajero_id** `uuid` — FK → `erp.empleados(id)`
- **caja_nombre** `text`
- **corte_nombre** `text`
- **tipo** `text` NOT NULL DEFAULT `'normal'::text`
- **estado** `text` NOT NULL DEFAULT `'abierto'::text`
- **efectivo_inicial** `numeric` DEFAULT `0`
- **efectivo_contado** `numeric`
- **total_ventas** `numeric` DEFAULT `0`
- **total_efectivo** `numeric` DEFAULT `0`
- **total_tarjeta** `numeric` DEFAULT `0`
- **total_transferencia** `numeric` DEFAULT `0`
- **diferencia** `numeric`
- **observaciones** `text`
- **fecha_operativa** `date` DEFAULT `CURRENT_DATE`
- **abierto_at** `timestamp with time zone` DEFAULT `now()`
- **cerrado_at** `timestamp with time zone`
- **validado_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.cortes_vouchers`

> Fotos de cierres de lote de terminales bancarias por corte. Feature 2026-04-24. MVP RDB; extensible.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **corte_id** `uuid` NOT NULL — FK → `erp.cortes_caja(id)`
- **storage_path** `text` NOT NULL
- **nombre_original** `text`
- **tamano_bytes** `integer`
- **mime_type** `text`
- **afiliacion** `text`
- **monto_reportado** `numeric`
- **uploaded_by** `uuid` — FK → `core.usuarios(id)`
- **uploaded_by_nombre** `text`
- **uploaded_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **categoria** `text` NOT NULL DEFAULT `'voucher_tarjeta'::text`
- **banco_id** `uuid` — FK → `core.bancos(id)`
- **movimiento_caja_id** `uuid` — FK → `erp.movimientos_caja(id)`
- **ocr_texto_crudo** `text`
- **ocr_monto_sugerido** `numeric`
- **ocr_banco_sugerido_id** `uuid` — FK → `core.bancos(id)`
- **ocr_confianza** `numeric`

### `erp.cotizacion_lineas`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cotizacion_id** `uuid` NOT NULL — FK → `erp.cotizaciones(id)`
- **partida_id** `uuid` — FK → `erp.presupuesto_partidas(id)`
- **descripcion** `text`
- **cantidad** `numeric` NOT NULL DEFAULT `1`
- **unidad** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cotizacion_proveedor_precios`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cotizacion_proveedor_id** `uuid` NOT NULL — FK → `erp.cotizacion_proveedores(id)`
- **cotizacion_linea_id** `uuid` NOT NULL — FK → `erp.cotizacion_lineas(id)`
- **precio_unitario** `numeric` NOT NULL DEFAULT `0`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cotizacion_proveedores`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cotizacion_id** `uuid` NOT NULL — FK → `erp.cotizaciones(id)`
- **proveedor_id** `uuid` NOT NULL — FK → `erp.proveedores(id)`
- **estado** `text` NOT NULL DEFAULT `'invitado'::text`
- **monto_total** `numeric`
- **tiempo_entrega** `text`
- **condiciones** `text`
- **adjunto_url** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cotizaciones`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text`
- **tipo** `text` NOT NULL DEFAULT `'compra'::text`
- **requisicion_id** `uuid` — FK → `erp.requisiciones(id)`
- **descripcion** `text`
- **estado** `text` NOT NULL DEFAULT `'abierta'::text`
- **fecha_limite** `date`
- **adjudicado_proveedor_id** `uuid` — FK → `erp.proveedores(id)`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `erp.cuenta_saldos`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cuenta_id** `uuid` NOT NULL — FK → `erp.cuentas_bancarias(id)`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **saldo** `numeric` NOT NULL
- **capturado_por** `uuid`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cuentas_bancarias`

> Cuentas bancarias por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **tipo** `text`
- **moneda_id** `uuid`
- **numero_cuenta** `text`
- **clabe** `text`
- **banco** `text`
- **saldo_actual** `numeric` DEFAULT `0`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.cxc_cargos`

> Cuentas por Cobrar — el adeudo (parcialidad de enganche, mensualidad, evento de crédito). Una fila por cargo del plan de pagos. ADR-037 D1.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **origen_tipo** `text` NOT NULL DEFAULT `'venta_dilesa'::text`
- **origen_id** `uuid`
- **tipo_cargo** `text` NOT NULL
- **numero** `integer` NOT NULL DEFAULT `1`
- **concepto** `text`
- **monto** `numeric` NOT NULL
- **monto_pagado** `numeric` NOT NULL DEFAULT `0`
- **saldo** `numeric`
- **fecha_vencimiento** `date`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **fuente_esperada** `text` NOT NULL DEFAULT `'cliente'::text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `erp.cxc_pago_aplicaciones`

> Aplicación N:M de un abono a uno o varios cargos. La invariante Σ aplicaciones del pago ≤ pago.monto_total se valida en la RPC de aplicación (PR A2). ADR-037 D1.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **pago_id** `uuid` NOT NULL — FK → `erp.cxc_pagos(id)`
- **cargo_id** `uuid` NOT NULL — FK → `erp.cxc_cargos(id)`
- **monto_aplicado** `numeric` NOT NULL
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cxc_pagos`

> Cuentas por Cobrar — el abono de un cliente o institución. Se aplica a 1..N cargos vía cxc_pago_aplicaciones. Absorbe dilesa.venta_pagos (módulo Coda Depositos Clientes). ADR-037 D1.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **monto_total** `numeric` NOT NULL
- **fuente** `text` NOT NULL DEFAULT `'cliente'::text`
- **forma_pago** `text`
- **referencia** `text`
- **cuenta_bancaria_id** `uuid` — FK → `erp.cuentas_bancarias(id)`
- **uuid_sat** `text`
- **comprobante_adjunto_id** `uuid`
- **notas** `text`
- **registrado_por** `uuid` DEFAULT `auth.uid()`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **coda_row_id** `text`
- **origen_tipo** `text` NOT NULL DEFAULT `'venta_dilesa'::text`
- **origen_id** `uuid`

### `erp.cxp_pago_aplicaciones`

> Aplicación N:M de un pago a una o varias facturas. La invariante Σ aplicaciones = pago.monto_total se valida en la RPC de programación. Espejo de erp.cxc_pago_aplicaciones. ADR-037 D1.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **pago_id** `uuid` NOT NULL — FK → `erp.cxp_pagos(id)`
- **factura_id** `uuid` NOT NULL — FK → `erp.facturas(id)`
- **monto_aplicado** `numeric` NOT NULL
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.cxp_pagos`

> Cuentas por Pagar — el pago a un proveedor. Se aplica a 1..N facturas vía cxp_pago_aplicaciones. Espejo de erp.cxc_pagos. ADR-037 D1.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proveedor_id** `uuid` — FK → `erp.personas(id)`
- **monto_total** `numeric` NOT NULL
- **fecha_programada** `date`
- **fecha_pago** `date`
- **cuenta_bancaria_id** `uuid` — FK → `erp.cuentas_bancarias(id)`
- **metodo_pago** `text`
- **referencia** `text`
- **estado** `text` NOT NULL DEFAULT `'programado'::text`
- **programado_por** `uuid` DEFAULT `auth.uid()`
- **aprobado_por** `uuid`
- **aprobado_at** `timestamp with time zone`
- **pagado_por** `uuid`
- **pagado_at** `timestamp with time zone`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `erp.departamentos`

> Estructura organizacional por empresa. Soporta jerarquía vía padre_id.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **codigo** `text`
- **padre_id** `uuid` — FK → `erp.departamentos(id)`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `erp.documentos`

> Escrituras, contratos y documentos legales. Fuente de verdad en ERP. Reemplaza core.documentos (ver migración 20260414000012).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **titulo** `text` NOT NULL
- **numero_documento** `text`
- **tipo** `text`
- **fecha_emision** `date`
- **fecha_vencimiento** `date`
- **notaria** `text`
- **notario_proveedor_id** `uuid` — FK → `erp.proveedores(id)`
- **notas** `text`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **archivo_url** `text`
- **subtipo_meta** `jsonb`
- **descripcion** `text`
- **contenido_texto** `text`
- **contenido_embedding** `USER-DEFINED`
- **extraccion_status** `text` NOT NULL DEFAULT `'pendiente'::text`
- **extraccion_fecha** `timestamp with time zone`
- **extraccion_modelo** `text`
- **extraccion_error** `text`
- **tipo_operacion** `text`
- **monto** `numeric`
- **moneda** `text` DEFAULT `'MXN'::text`
- **superficie_m2** `numeric`
- **ubicacion_predio** `text`
- **municipio** `text`
- **estado** `text`
- **folio_real** `text`
- **libro_tomo** `text`
- **partes** `jsonb`
- **contenido_texto_tsv** `tsvector`
- **precio_m2** `numeric`

### `erp.empleado_beneficiarios`

> Beneficiarios designados por el empleado (Art. 501 LFT).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **empleado_id** `uuid` NOT NULL — FK → `erp.empleados(id)`
- **nombre** `text` NOT NULL
- **parentesco** `text`
- **porcentaje** `numeric`
- **orden** `integer` NOT NULL DEFAULT `1`
- **telefono** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.empleados`

> Empleados por empresa. La identidad base vive en erp.personas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **departamento_id** `uuid` — FK → `erp.departamentos(id)`
- **puesto_id** `uuid` — FK → `erp.puestos(id)`
- **numero_empleado** `text`
- **fecha_ingreso** `date`
- **fecha_baja** `date`
- **motivo_baja** `text`
- **reemplaza_a** `uuid` — FK → `erp.empleados(id)`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **nss** `text`
- **fecha_nacimiento** `date`
- **telefono_empresa** `text`
- **extension** `text`
- **email_empresa** `text`
- **notas** `text`
- **tipo_contrato** `text`
- **periodo_prueba_dias** `integer`
- **periodo_prueba_numero** `integer`
- **horario** `text`
- **lugar_trabajo** `text`
- **dia_pago** `text`
- **funciones** `text`
- **umf** `text`
- **zona_salario** `text`
- **regimen_imss** `text`
- **tipo_prestacion** `text`
- **sindicalizado** `text`
- **metodo_pago_sat** `text`

### `erp.empleados_compensacion`

> Historial de compensaciones. Solo vigente=true es el registro activo.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **empleado_id** `uuid` NOT NULL — FK → `erp.empleados(id)`
- **sueldo_diario** `numeric`
- **sueldo_mensual** `numeric`
- **tipo_contrato** `text`
- **frecuencia_pago** `text`
- **fecha_inicio** `date` NOT NULL
- **fecha_fin** `date`
- **vigente** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **comisiones_mensuales** `numeric` DEFAULT `0`
- **bonificaciones_mensuales** `numeric` DEFAULT `0`
- **compensaciones_mensuales** `numeric` DEFAULT `0`
- **sdi** `numeric`

### `erp.empleados_import_log`

> Audit trail del import de empleados desde CONTPAQi (o futuras fuentes). Una fila por acción aplicada al empleado/persona en cada snapshot. diff jsonb captura el cambio exacto. Iniciativa: import-empleados-contpaqi.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **empleado_id** `uuid` — FK → `erp.empleados(id)`
- **persona_id** `uuid` — FK → `erp.personas(id)`
- **snapshot_fecha** `date` NOT NULL
- **origen** `text` NOT NULL
- **accion** `text` NOT NULL
- **match_metodo** `text`
- **diff** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.empleados_pago`

> Cuenta bancaria de pago de nómina por empleado. 1:N con vigente=true para el activo, mismo patrón que erp.empleados_compensacion. Histórico de cambios de cuenta queda en filas con vigente=false. Iniciativa: import-empleados-contpaqi.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **empleado_id** `uuid` NOT NULL — FK → `erp.empleados(id)`
- **banco_codigo** `text`
- **banco_nombre** `text`
- **numero_cuenta** `text`
- **clabe** `text`
- **sucursal** `text`
- **vigente** `boolean` NOT NULL DEFAULT `true`
- **fecha_inicio** `date`
- **fecha_fin** `date`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.empleados_puestos`

> Relación N:M entre empleados y puestos. Permite que una persona-empresa tenga múltiples roles (Accionista + Comité + Consejo, etc.) sin duplicar la fila en erp.empleados. Ver ADR-013.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **empleado_id** `uuid` NOT NULL — FK → `erp.empleados(id)`
- **puesto_id** `uuid` NOT NULL — FK → `erp.puestos(id)`
- **principal** `boolean` NOT NULL DEFAULT `false`
- **fecha_inicio** `date`
- **fecha_fin** `date`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.facturas`

> Facturas SAT (ingreso y egreso) por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **uuid_sat** `text`
- **flujo** `text` NOT NULL
- **tipo_ingreso_id** `uuid`
- **persona_id** `uuid` — FK → `erp.personas(id)`
- **emisor_rfc** `text`
- **emisor_nombre** `text`
- **receptor_rfc** `text`
- **subtotal** `numeric`
- **iva** `numeric`
- **total** `numeric`
- **fecha_emision** `date` NOT NULL
- **fecha_vencimiento** `date`
- **xml_url** `text`
- **pdf_url** `text`
- **estado_id** `uuid`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **orden_compra_id** `uuid` — FK → `erp.ordenes_compra(id)`
- **proveedor_id** `uuid` — FK → `erp.personas(id)`
- **condiciones_pago_dias** `integer`
- **fecha_pago_programada** `date`
- **monto_pagado** `numeric` NOT NULL DEFAULT `0`
- **saldo** `numeric`
- **estado_cxp** `text` NOT NULL DEFAULT `'borrador'::text`
- **forma_pago_sat** `text`
- **metodo_pago_sat** `text`
- **uso_cfdi** `text`
- **tasa_iva** `numeric`
- **retencion_iva** `numeric` NOT NULL DEFAULT `0`
- **retencion_isr** `numeric` NOT NULL DEFAULT `0`
- **motivo_cancelacion** `text`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **obra_estimacion_id** `uuid` — FK → `dilesa.obra_estimaciones(id)`
- **partida_id** `uuid` — FK → `erp.presupuesto_partidas(id)`

### `erp.finiquitos`

> Snapshot inmutable de cada convenio de terminación laboral generado en BSOP. Una fila por finiquito firmado/impreso. UPDATE/DELETE solo admin.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empleado_id** `uuid` NOT NULL — FK → `erp.empleados(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **fecha_baja** `date` NOT NULL
- **fecha_convenio** `date` NOT NULL
- **causa** `text` NOT NULL
- **motivo_detalle** `text`
- **fecha_ingreso** `date` NOT NULL
- **antiguedad_anios** `integer` NOT NULL
- **antiguedad_meses** `integer` NOT NULL
- **antiguedad_dias** `integer` NOT NULL
- **sueldo_diario** `numeric` NOT NULL
- **sdi** `numeric`
- **salario_minimo_diario** `numeric` NOT NULL
- **zona_salario_minimo** `text` NOT NULL
- **total_finiquito** `numeric` NOT NULL
- **total_indemnizacion** `numeric` NOT NULL DEFAULT `0`
- **total_general** `numeric` NOT NULL
- **conceptos** `jsonb` NOT NULL
- **notas_calculo** `jsonb` NOT NULL DEFAULT `'[]'::jsonb`
- **empleado_snapshot** `jsonb` NOT NULL
- **patron_snapshot** `jsonb` NOT NULL
- **forma_pago** `text` NOT NULL
- **referencia_pago** `text`
- **creado_por** `uuid`
- **creado_en** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.gastos`

> Registro de gastos operativos por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **categoria_id** `uuid`
- **descripcion** `text` NOT NULL
- **monto** `numeric` NOT NULL
- **moneda_id** `uuid`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **metodo_pago** `text`
- **referencia** `text`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **registrado** `boolean` NOT NULL DEFAULT `false`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.inventario`

> Stock actual por producto y almacén. Se actualiza mediante movimientos_inventario.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **producto_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **almacen_id** `uuid` NOT NULL — FK → `erp.almacenes(id)`
- **cantidad** `numeric` NOT NULL DEFAULT `0`
- **cantidad_minima** `numeric`
- **cantidad_maxima** `numeric`
- **costo_promedio** `numeric`
- **ultimo_movimiento** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.inventario_levantamiento_firmas`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **levantamiento_id** `uuid` NOT NULL — FK → `erp.inventario_levantamientos(id)`
- **paso** `integer` NOT NULL
- **rol** `text` NOT NULL
- **firmante_id** `uuid` NOT NULL — FK → `core.usuarios(id)`
- **firmante_nombre** `text` NOT NULL
- **firmado_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **comentario** `text`
- **total_lineas** `integer` NOT NULL
- **total_diferencia** `numeric`
- **total_lineas_fuera** `integer` NOT NULL
- **ip** `inet`
- **user_agent** `text`

### `erp.inventario_levantamiento_lineas`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **levantamiento_id** `uuid` NOT NULL — FK → `erp.inventario_levantamientos(id)`
- **producto_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **stock_inicial** `numeric` NOT NULL
- **costo_unitario** `numeric`
- **salidas_durante_captura** `numeric` NOT NULL DEFAULT `0`
- **stock_efectivo** `numeric`
- **cantidad_contada** `numeric`
- **contado_por** `uuid` — FK → `core.usuarios(id)`
- **contado_at** `timestamp with time zone`
- **recontada** `boolean` NOT NULL DEFAULT `false`
- **diferencia** `numeric`
- **diferencia_valor** `numeric`
- **fuera_de_tolerancia** `boolean` NOT NULL DEFAULT `false`
- **notas_diferencia** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.inventario_levantamientos`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **almacen_id** `uuid` NOT NULL — FK → `erp.almacenes(id)`
- **folio** `text`
- **tipo** `text` NOT NULL DEFAULT `'total'::text`
- **estado** `text` NOT NULL DEFAULT `'borrador'::text`
- **fecha_programada** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **fecha_inicio** `timestamp with time zone`
- **fecha_cierre** `timestamp with time zone`
- **fecha_aplicado** `timestamp with time zone`
- **fecha_cancelado** `timestamp with time zone`
- **contador_id** `uuid` — FK → `core.usuarios(id)`
- **tolerancia_pct_override** `numeric`
- **tolerancia_monto_override** `numeric`
- **notas** `text`
- **motivo_cancelacion** `text`
- **created_by** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`

### `erp.juntas`

> Juntas operativas internas. Tiene hijos: juntas_asistencia y juntas_notas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **titulo** `text` NOT NULL
- **descripcion** `text`
- **fecha_hora** `timestamp with time zone` NOT NULL
- **duracion_minutos** `integer` DEFAULT `60`
- **lugar** `text`
- **estado** `text` NOT NULL DEFAULT `'programada'::text`
- **tipo** `text`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **fecha_terminada** `timestamp with time zone`
- **enviar_a_consejo** `boolean` NOT NULL DEFAULT `true`

### `erp.juntas_asistencia`

> Registro de asistencia a juntas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **junta_id** `uuid` NOT NULL — FK → `erp.juntas(id)`
- **persona_id** `uuid` — FK → `erp.personas(id)`
- **asistio** `boolean`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.juntas_notas`

> Notas/minutas de una junta, ordenadas por orden.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **junta_id** `uuid` NOT NULL — FK → `erp.juntas(id)`
- **orden** `integer` NOT NULL DEFAULT `1`
- **contenido** `text` NOT NULL
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.lotes`

> Lotes dentro de un proyecto inmobiliario.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` NOT NULL — FK → `erp.proyectos(id)`
- **codigo** `text`
- **manzana** `text`
- **lote** `text`
- **superficie_m2** `numeric`
- **precio_lista** `numeric`
- **precio_venta** `numeric`
- **estado** `text` NOT NULL DEFAULT `'disponible'::text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.movimientos_bancarios`

> Movimientos bancarios (cargos y abonos) por cuenta y empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cuenta_id** `uuid` NOT NULL — FK → `erp.cuentas_bancarias(id)`
- **tipo** `text` NOT NULL
- **monto** `numeric` NOT NULL
- **moneda_id** `uuid`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **descripcion** `text`
- **referencia** `text`
- **categoria_id** `uuid`
- **conciliado** `boolean` NOT NULL DEFAULT `false`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **referencia_tipo** `text`
- **referencia_id** `uuid`

### `erp.movimientos_caja`

> Entradas y salidas manuales de caja no ligadas a ventas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **corte_id** `uuid` — FK → `erp.cortes_caja(id)`
- **tipo** `text` NOT NULL
- **monto** `numeric` NOT NULL
- **concepto** `text`
- **referencia** `text`
- **realizado_por** `uuid` — FK → `erp.empleados(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **tipo_detalle** `text`
- **realizado_por_nombre** `text`

### `erp.movimientos_inventario`

> Cada movimiento de inventario. referencia_tipo/id trazan el origen (OC, requisición, etc).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **producto_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **almacen_id** `uuid` NOT NULL — FK → `erp.almacenes(id)`
- **tipo_movimiento** `text` NOT NULL
- **cantidad** `numeric` NOT NULL
- **costo_unitario** `numeric`
- **referencia_tipo** `text`
- **referencia_id** `uuid`
- **notas** `text`
- **created_by** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.ordenes_compra`

> Órdenes de compra emitidas a proveedores. Pueden derivar de una requisición o crearse directamente.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **requisicion_id** `uuid` — FK → `erp.requisiciones(id)`
- **proveedor_id** `uuid` — FK → `erp.proveedores(id)`
- **codigo** `text`
- **estado_id** `uuid`
- **moneda_id** `uuid`
- **subtotal** `numeric`
- **iva** `numeric`
- **total** `numeric`
- **condiciones_pago** `text`
- **fecha_entrega** `date`
- **direccion_entrega** `text`
- **autorizada_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **estado** `text` NOT NULL DEFAULT `'borrador'::text`
- **total_a_pagar** `numeric`
- **cerrada_at** `timestamp with time zone`
- **cerrada_por** `uuid` — FK → `core.usuarios(id)`
- **cotizacion_id** `uuid` — FK → `erp.cotizaciones(id)`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `erp.ordenes_compra_detalle`

> Líneas de una orden de compra.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **orden_compra_id** `uuid` NOT NULL — FK → `erp.ordenes_compra(id)`
- **producto_id** `uuid` — FK → `erp.productos(id)`
- **descripcion** `text`
- **unidad** `text`
- **cantidad** `numeric` NOT NULL DEFAULT `1`
- **precio_unitario** `numeric`
- **descuento** `numeric` DEFAULT `0`
- **subtotal** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **cantidad_recibida** `numeric` NOT NULL DEFAULT `0`
- **cantidad_cancelada** `numeric` NOT NULL DEFAULT `0`
- **precio_real** `numeric`
- **precio_modificado_por** `uuid` — FK → `core.usuarios(id)`
- **precio_modificado_at** `timestamp with time zone`
- **motivo_cancelacion** `text`
- **partida_id** `uuid` — FK → `erp.presupuesto_partidas(id)`

### `erp.pagos`

> Pagos registrados contra un cargo de cobranza.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cobranza_id** `uuid` NOT NULL — FK → `erp.cobranza(id)`
- **monto** `numeric` NOT NULL
- **metodo** `text`
- **referencia** `text`
- **fecha_pago** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **recibio_id** `uuid` — FK → `erp.empleados(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.pagos_provisionales`

> Control de pagos provisionales ISR por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **anio_fiscal** `integer` NOT NULL
- **mes** `integer` NOT NULL
- **base_gravable** `numeric`
- **isr_calculado** `numeric`
- **pago** `numeric`
- **fecha_pago** `date`
- **comprobante_url** `text`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.personas`

> Registro maestro de personas físicas/morales. Base para empleados, proveedores y clientes.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **apellido_paterno** `text`
- **apellido_materno** `text`
- **email** `text`
- **telefono** `text`
- **rfc** `text`
- **curp** `text`
- **tipo** `text` NOT NULL DEFAULT `'general'::text`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **nss** `text`
- **fecha_nacimiento** `date`
- **telefono_casa** `text`
- **contacto_emergencia_nombre** `text`
- **contacto_emergencia_telefono** `text`
- **contacto_emergencia_parentesco** `text`
- **domicilio** `text`
- **nacionalidad** `text` DEFAULT `'Mexicana'::text`
- **estado_civil** `text`
- **sexo** `text`
- **lugar_nacimiento** `text`
- **tipo_persona** `text` NOT NULL DEFAULT `'fisica'::text`
- **numero_credencial_ine** `text`
- **domicilio_calle** `text`
- **domicilio_numero_exterior** `text`
- **domicilio_numero_interior** `text`
- **domicilio_colonia** `text`
- **domicilio_codigo_postal** `text`
- **domicilio_ciudad** `text`
- **domicilio_estado** `text`
- **es_pep** `boolean` DEFAULT `false`
- **forma_pago_kyc** `text`
- **uso_efectivo_kyc** `text`
- **conocimiento_dueno_beneficiario** `text` DEFAULT `'No'::text`
- **ocupacion** `text`

### `erp.personas_contactos`

> Multi-contacto operativo por persona (proveedor/empleado/cliente). El contacto fiscal vive en erp.personas_datos_fiscales. ADR-028.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **nombre** `text` NOT NULL
- **puesto** `text`
- **telefono** `text`
- **email** `text`
- **notas** `text`
- **principal** `boolean` NOT NULL DEFAULT `false`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.personas_cuentas_bancarias`

> Multi-cuenta bancaria de terceros (proveedor/empleado/cliente). NO confundir con erp.cuentas_bancarias (cuentas propias de la empresa con saldo). ADR-028.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **banco_id** `uuid` — FK → `core.bancos(id)`
- **banco_nombre** `text`
- **numero_cuenta** `text`
- **clabe** `text`
- **tipo** `text`
- **moneda** `text` NOT NULL DEFAULT `'MXN'::text`
- **vigente** `boolean` NOT NULL DEFAULT `true`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.personas_datos_fiscales`

> Datos fiscales de personas (CSF parseada). 1:1 con erp.personas. Solo se crea row cuando hay CSF cargada — personas sin CSF no tienen fila aquí. Histórico de CSFs vive en erp.adjuntos (entidad_tipo='persona', rol='csf'); csf_adjunto_id apunta a la vigente.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **razon_social** `text`
- **nombre_comercial** `text`
- **regimen_fiscal_codigo** `text`
- **regimen_fiscal_nombre** `text`
- **regimenes_adicionales** `jsonb`
- **domicilio_calle** `text`
- **domicilio_num_ext** `text`
- **domicilio_num_int** `text`
- **domicilio_colonia** `text`
- **domicilio_cp** `text`
- **domicilio_municipio** `text`
- **domicilio_estado** `text`
- **domicilio_pais** `text` DEFAULT `'México'::text`
- **obligaciones** `jsonb`
- **csf_adjunto_id** `uuid` — FK → `erp.adjuntos(id)`
- **csf_fecha_emision** `date`
- **fecha_inicio_operaciones** `date`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.personas_direcciones`

> Multi-dirección operativa por persona. El domicilio fiscal vive en erp.personas_datos_fiscales (validado vía CSF). ADR-028.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **tipo** `text` NOT NULL DEFAULT `'operativo'::text`
- **calle** `text`
- **num_ext** `text`
- **num_int** `text`
- **colonia** `text`
- **cp** `text`
- **municipio** `text`
- **estado** `text`
- **pais** `text` NOT NULL DEFAULT `'México'::text`
- **referencia** `text`
- **principal** `boolean` NOT NULL DEFAULT `false`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.presupuesto_partidas`

> Modelo canónico de partidas de presupuesto (obra + futuro checklist). Vive en erp para binding intra-schema con líneas de compra. proyecto_id es cross-schema a dilesa.proyectos (metadata). ADR-040.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **proyecto_id** `uuid` — FK → `dilesa.proyectos(id)`
- **concepto_id** `uuid` — FK → `erp.conceptos_compra(id)`
- **concepto_texto** `text`
- **etapa** `text`
- **tipo_insumo** `text`
- **orden** `integer` NOT NULL DEFAULT `0`
- **presupuesto_previo** `numeric`
- **presupuesto_aprobado** `numeric`
- **monto_estimado** `numeric`
- **gasto_real_subtotal** `numeric`
- **gasto_real_iva** `numeric`
- **gasto_real_total** `numeric`
- **gasto_real_iva_tasa** `numeric`
- **estado** `text` NOT NULL DEFAULT `'planeada'::text`
- **fuente** `text`
- **proveedor_persona_id** `uuid` — FK → `erp.personas(id)`
- **proveedor_texto** `text`
- **contrato_id** `uuid` — FK → `dilesa.contratos_construccion(id)`
- **tarea_origen_id** `uuid`
- **factura_ref** `text`
- **fecha_compromiso** `date`
- **autorizado_at** `timestamp with time zone`
- **autorizado_por** `uuid`
- **notas** `text`
- **source_ref** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **deleted_at** `timestamp with time zone`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `erp.producto_receta`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **producto_venta_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **insumo_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **cantidad** `numeric` NOT NULL
- **unidad** `text` NOT NULL
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.productos`

> Catálogo maestro de productos, servicios e insumos por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **categoria_id** `uuid` — FK → `erp.categorias_producto(id)`
- **codigo** `text`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **tipo** `text` NOT NULL DEFAULT `'producto'::text`
- **unidad** `text` NOT NULL DEFAULT `'pieza'::text`
- **inventariable** `boolean` NOT NULL DEFAULT `true`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **parent_id** `uuid` — FK → `erp.productos(id)`
- **factor_consumo** `numeric` NOT NULL DEFAULT `1.0`
- **clasificacion** `USER-DEFINED` DEFAULT `'inventariable'::erp.clasificacion_producto`

### `erp.productos_precios`

> Historial de costos y precios. Solo vigente=true es el precio activo.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **producto_id** `uuid` NOT NULL — FK → `erp.productos(id)`
- **costo** `numeric`
- **precio_venta** `numeric`
- **fecha_inicio** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **fecha_fin** `date`
- **vigente** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.proveedores`

> Catálogo de proveedores vinculados a erp.personas.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **persona_id** `uuid` NOT NULL — FK → `erp.personas(id)`
- **codigo** `text`
- **condiciones_pago** `text`
- **limite_credito** `numeric`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **categoria** `text`
- **tasa_iva** `numeric`

### `erp.proyectos`

> Proyectos inmobiliarios de DILESA.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text`
- **nombre** `text` NOT NULL
- **descripcion** `text`
- **tipo** `text`
- **ubicacion** `text`
- **estado_id** `uuid`
- **presupuesto** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.puestos`

> Catálogo de puestos por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **departamento_id** `uuid` — FK → `erp.departamentos(id)`
- **nombre** `text` NOT NULL
- **nivel** `text`
- **sueldo_min** `numeric`
- **sueldo_max** `numeric`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **objetivo** `text`
- **perfil** `text`
- **requisitos** `text`
- **esquema_pago** `text`
- **reporta_a** `uuid` — FK → `erp.puestos(id)`
- **deleted_at** `timestamp with time zone`

### `erp.recepciones`

> Registro de recepción física de mercancía contra OC.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **orden_compra_id** `uuid` — FK → `erp.ordenes_compra(id)`
- **recibe_id** `uuid` — FK → `erp.empleados(id)`
- **codigo** `text`
- **estado_id** `uuid`
- **fecha_recepcion** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.recepciones_detalle`

> Cantidades recibidas vs esperadas por línea de OC.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **recepcion_id** `uuid` NOT NULL — FK → `erp.recepciones(id)`
- **oc_detalle_id** `uuid` — FK → `erp.ordenes_compra_detalle(id)`
- **producto_id** `uuid` — FK → `erp.productos(id)`
- **cantidad_esperada** `numeric`
- **cantidad_recibida** `numeric` NOT NULL DEFAULT `0`
- **cantidad_rechazada** `numeric` NOT NULL DEFAULT `0`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.requisiciones`

> Solicitudes de compra por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **codigo** `text`
- **solicitante_id** `uuid` — FK → `core.usuarios(id)`
- **departamento_id** `uuid` — FK → `erp.departamentos(id)`
- **prioridad_id** `uuid`
- **estado_id** `uuid`
- **subtipo** `text`
- **justificacion** `text`
- **fecha_requerida** `date`
- **autorizada_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **deleted_at** `timestamp with time zone`
- **cancelada_at** `timestamp with time zone`
- **cancelada_por** `uuid`
- **motivo_cancelacion** `text`

### `erp.requisiciones_detalle`

> Líneas de una requisición. producto_id puede ser NULL para artículos de texto libre.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **requisicion_id** `uuid` NOT NULL — FK → `erp.requisiciones(id)`
- **producto_id** `uuid` — FK → `erp.productos(id)`
- **descripcion** `text`
- **unidad** `text`
- **cantidad** `numeric` NOT NULL DEFAULT `1`
- **precio_estimado** `numeric`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **partida_id** `uuid` — FK → `erp.presupuesto_partidas(id)`

### `erp.taller_servicio`

> Órdenes de servicio del taller ANSA.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **vehiculo_id** `uuid` — FK → `erp.vehiculos(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **tecnico_id** `uuid` — FK → `erp.empleados(id)`
- **tipo** `text`
- **descripcion** `text`
- **total** `numeric`
- **estado_id** `uuid`
- **fecha_entrada** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **fecha_entrega** `date`
- **vin_externo** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.task_updates`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **task_id** `uuid` NOT NULL — FK → `erp.tasks(id)`
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **tipo** `text` NOT NULL
- **contenido** `text`
- **valor_anterior** `text`
- **valor_nuevo** `text`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **junta_id** `uuid` — FK → `erp.juntas(id)`

### `erp.tasks`

> Tareas operativas por empresa.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **titulo** `text` NOT NULL
- **descripcion** `text`
- **asignado_a** `uuid` — FK → `erp.empleados(id)`
- **creado_por** `uuid` — FK → `core.usuarios(id)`
- **prioridad_id** `uuid`
- **estado** `text` NOT NULL DEFAULT `'pendiente'::text`
- **fecha_vence** `date`
- **entidad_tipo** `text`
- **entidad_id** `uuid`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`
- **asignado_por** `uuid` — FK → `erp.empleados(id)`
- **fecha_completado** `timestamp with time zone`
- **completado_por** `uuid` — FK → `erp.empleados(id)`
- **porcentaje_avance** `integer` DEFAULT `0`
- **fecha_compromiso** `date`
- **tipo** `text`
- **motivo_bloqueo** `text`
- **siguiente_accion** `text`
- **iniciativa** `text`
- **departamento_nombre** `text`
- **prioridad** `text`
- **cierre_solicitado_en** `timestamp with time zone`
- **cierre_solicitado_por** `uuid` — FK → `erp.empleados(id)`
- **cierre_aprobado_en** `timestamp with time zone`
- **cierre_aprobado_por** `uuid` — FK → `erp.empleados(id)`
- **cierre_rechazado_en** `timestamp with time zone`
- **cierre_rechazado_por** `uuid` — FK → `erp.empleados(id)`
- **cierre_rechazado_motivo** `text`

### `erp.turnos`

> Catálogo de turnos por empresa (mañana, tarde, noche, etc).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **nombre** `text` NOT NULL
- **hora_inicio** `time without time zone`
- **hora_fin** `time without time zone`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.v_cuenta_saldo_actual` _(view)_

- **cuenta_id** `uuid`
- **empresa_id** `uuid`
- **banco** `text`
- **nombre** `text`
- **moneda_id** `uuid`
- **saldo** `numeric`
- **fecha_saldo** `date`
- **capturado_at** `timestamp with time zone`

### `erp.v_empleados_full` _(view)_

- **empleado_id** `uuid`
- **empresa_id** `uuid`
- **numero_empleado** `text`
- **fecha_ingreso** `date`
- **fecha_baja** `date`
- **motivo_baja** `text`
- **email_empresa** `text`
- **empleado_activo** `boolean`
- **persona_id** `uuid`
- **nombre** `text`
- **apellido_paterno** `text`
- **apellido_materno** `text`
- **nombre_completo** `text`
- **email_personal** `text`
- **telefono** `text`
- **rfc** `text`
- **curp** `text`
- **nss** `text`
- **fecha_nacimiento** `date`
- **edad** `numeric`
- **departamento_id** `uuid`
- **departamento** `text`
- **puesto_id** `uuid`
- **puesto** `text`
- **puestos** `jsonb`
- **sueldo_diario** `numeric`
- **sueldo_mensual** `numeric`
- **comisiones_mensuales** `numeric`
- **bonificaciones_mensuales** `numeric`
- **compensaciones_mensuales** `numeric`
- **total_percepciones_mensuales** `numeric`
- **sdi** `numeric`
- **tipo_contrato** `text`
- **frecuencia_pago** `text`
- **antiguedad_anios** `numeric`

### `erp.v_partida_control` _(view)_

> Control presupuestal 3 capas por partida: comprometido (OC activas) / ejercido (recibido) / pagado (aplicaciones de pago) + disponible. gasto_real_manual es la captura histórica de obra. ADR-040.

- **partida_id** `uuid`
- **empresa_id** `uuid`
- **proyecto_id** `uuid`
- **concepto_id** `uuid`
- **concepto_texto** `text`
- **etapa** `text`
- **estado** `text`
- **presupuesto_aprobado** `numeric`
- **comprometido** `numeric`
- **ejercido** `numeric`
- **pagado** `numeric`
- **gasto_real_manual** `numeric`
- **disponible** `numeric`

### `erp.vehiculos`

> Inventario de vehículos ANSA (Stellantis: Chrysler, Dodge, Jeep, Ram, Fiat, Peugeot).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **vin** `text`
- **marca** `text` NOT NULL
- **modelo** `text` NOT NULL
- **anio** `integer`
- **color** `text`
- **precio_lista** `numeric`
- **estado** `text` NOT NULL DEFAULT `'disponible'::text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.ventas_autos`

> Ventas de vehículos de ANSA.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **vehiculo_id** `uuid` NOT NULL — FK → `erp.vehiculos(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **vendedor_id** `uuid` — FK → `erp.empleados(id)`
- **tipo** `text` NOT NULL DEFAULT `'contado'::text`
- **precio_venta** `numeric` NOT NULL
- **comision** `numeric`
- **estado_id** `uuid`
- **fecha_venta** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.ventas_inmobiliarias`

> Ventas de lotes a clientes.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **lote_id** `uuid` NOT NULL — FK → `erp.lotes(id)`
- **vendedor_id** `uuid` — FK → `erp.empleados(id)`
- **codigo** `text`
- **precio_venta** `numeric` NOT NULL
- **enganche** `numeric` DEFAULT `0`
- **plazo_meses** `integer` DEFAULT `0`
- **estado_id** `uuid`
- **fecha_venta** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

### `erp.ventas_refacciones_detalle`

> Líneas de detalle de un ticket de refacciones.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **ticket_id** `uuid` NOT NULL — FK → `erp.ventas_tickets(id)`
- **producto_id** `uuid` — FK → `erp.productos(id)`
- **descripcion** `text`
- **cantidad** `numeric` NOT NULL DEFAULT `1`
- **precio_unitario** `numeric`
- **descuento** `numeric` DEFAULT `0`
- **total** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `erp.ventas_tickets`

> Tickets de venta de refacciones y servicios de mostrador.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **empresa_id** `uuid` NOT NULL — FK → `core.empresas(id)`
- **cliente_id** `uuid` — FK → `erp.clientes(id)`
- **vendedor_id** `uuid` — FK → `erp.empleados(id)`
- **codigo** `text`
- **total** `numeric` NOT NULL DEFAULT `0`
- **estado_id** `uuid`
- **fecha** `date` NOT NULL DEFAULT `CURRENT_DATE`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone`

---

## Schema `health`

### `health.health_ecg`

- **id** `bigint` NOT NULL DEFAULT `nextval('health.health_ecg_id_seq'::regclass)` — PK
- **date** `timestamp with time zone` NOT NULL
- **classification** `text`
- **heart_rate** `real`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone` DEFAULT `now()`

### `health.health_ingest_log`

- **id** `bigint` NOT NULL DEFAULT `nextval('health.health_ingest_log_id_seq'::regclass)` — PK
- **received_at** `timestamp with time zone` DEFAULT `now()`
- **payload_size_bytes** `integer`
- **metrics_count** `integer` DEFAULT `0`
- **workouts_count** `integer` DEFAULT `0`
- **source_ip** `text`
- **status** `text` DEFAULT `'ok'::text`
- **metrics_by_name** `jsonb` NOT NULL DEFAULT `'{}'::jsonb`

### `health.health_medications`

- **id** `bigint` NOT NULL DEFAULT `nextval('health.health_medications_id_seq'::regclass)` — PK
- **date** `timestamp with time zone` NOT NULL
- **name** `text`
- **dose** `text`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone` DEFAULT `now()`

### `health.health_metrics`

- **id** `bigint` NOT NULL DEFAULT `nextval('health.health_metrics_id_seq'::regclass)` — PK
- **metric_name** `text` NOT NULL
- **date** `timestamp with time zone` NOT NULL
- **value** `real` NOT NULL
- **unit** `text`
- **source** `text`
- **ingested_at** `timestamp with time zone` DEFAULT `now()`

### `health.health_workouts`

- **id** `bigint` NOT NULL DEFAULT `nextval('health.health_workouts_id_seq'::regclass)` — PK
- **name** `text` NOT NULL
- **start_time** `timestamp with time zone` NOT NULL
- **end_time** `timestamp with time zone`
- **duration_minutes** `real`
- **distance_km** `real`
- **energy_kcal** `real`
- **heart_rate_avg** `real`
- **heart_rate_max** `real`
- **source** `text`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone` DEFAULT `now()`

### `health.protocolo_compuestos`

> Catálogo de compuestos que Beto se administra (péptidos, suplementos, orales). Iniciativa salud-protocolo.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **nombre** `text` NOT NULL
- **clase** `text` NOT NULL
- **via** `text`
- **unidad_dosis** `text`
- **dosis_objetivo** `numeric`
- **frecuencia** `text`
- **procedencia** `text`
- **estado** `text` NOT NULL DEFAULT `'activo'::text`
- **fecha_inicio** `date`
- **fecha_fin** `date`
- **color** `text`
- **notas** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **componentes** `jsonb`

### `health.protocolo_efectos`

> Cómo cae un compuesto: escalas 0–5 (apetito/energía: 0 bajo → 5 alto; náusea/GI: 0 nada → 5 severo) + nota. toma_id opcional liga el efecto a una administración.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **fecha** `timestamp with time zone` NOT NULL
- **toma_id** `uuid` — FK → `health.protocolo_tomas(id)`
- **apetito** `smallint`
- **nausea** `smallint`
- **energia** `smallint`
- **gi** `smallint`
- **nota** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `health.protocolo_tomas`

> Cada administración real de un compuesto (la bitácora). dosis = dosis real aplicada (ground-truth de titración); sitio = rotación de inyección (null para orales).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **compuesto_id** `uuid` NOT NULL — FK → `health.protocolo_compuestos(id)`
- **fecha** `timestamp with time zone` NOT NULL
- **dosis** `numeric` NOT NULL
- **unidad** `text`
- **sitio** `text`
- **nota** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **vial_mg** `numeric`
- **bac_ml** `numeric`
- **concentracion** `numeric`
- **unidades** `numeric`

---

## Schema `maquinaria`

_(sin tablas ni vistas)_

---

## Schema `peptides`

### `peptides.insumos`

> Proveedores de insumos (bac water, viales, jeringas). Snapshot-replace. Iniciativa sanren-peptides.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **proveedor** `text` NOT NULL
- **url** `text`
- **productos** `text`
- **imported_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `peptides.notas`

> Hallazgos/alertas curados (Telegram/wiki/doc/manual), taggeables y ligables a péptido/vendor. Iniciativa sanren-peptides.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **titulo** `text`
- **cuerpo** `text`
- **tags** `ARRAY`
- **tipo** `text`
- **peptido** `text`
- **vendor_codigo** `text`
- **fuente** `text`
- **fecha** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `peptides.peptidos`

> Catálogo de referencia de péptidos (curado). Iniciativa sanren-peptides.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **nombre** `text` NOT NULL
- **aliases** `ARRAY`
- **clase** `text`
- **descripcion** `text`
- **protocolo_tipico** `text`
- **reconstitucion** `text`
- **cautelas** `text`
- **fuente** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `peptides.tests`

> COA/testing por batch (pureza/endotoxina/masa). Links blandos por texto. Snapshot-replace. Iniciativa sanren-peptides.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **vendor_codigo** `text`
- **peptido** `text`
- **test_date** `date`
- **batch** `text`
- **expected_mass_mg** `numeric`
- **mass_mg** `numeric`
- **purity_pct** `numeric`
- **tfa** `text`
- **endotoxin** `text`
- **test_lab** `text`
- **file_name** `text`
- **lab_url** `text`
- **imported_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `peptides.vendors`

> Vendors de péptidos (snapshot sheets STG). notas = historial/warnings; nota_personal = de Beto (sobrevive re-import). Iniciativa sanren-peptides.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **codigo** `text` NOT NULL
- **nombre** `text`
- **estado** `text` NOT NULL DEFAULT `'activo'::text`
- **precio_mg** `numeric`
- **precio_mg_sale** `numeric`
- **moneda** `text` DEFAULT `'USD'::text`
- **us_warehouse** `boolean`
- **china_warehouse** `boolean`
- **eu_warehouse** `boolean`
- **metodos_pago** `text`
- **primer_contacto** `text`
- **garantia** `text`
- **notas** `text`
- **nota_personal** `text`
- **fuente_url** `text`
- **imported_at** `timestamp with time zone`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

---

## Schema `playtomic`

### `playtomic.booking_participants`

> Populated by edge function playtomic-sync using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **booking_id** `text` NOT NULL — FK → `playtomic.bookings(booking_id)`
- **player_id** `text` NOT NULL — FK → `playtomic.players(playtomic_id)`
- **is_owner** `boolean` NOT NULL DEFAULT `false`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **family_member_id** `text`

### `playtomic.bookings`

> Populated by edge function playtomic-sync using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **booking_id** `text` NOT NULL
- **object_id** `text`
- **resource_id** `text`
- **resource_name** `text`
- **sport_id** `text`
- **booking_start** `timestamp with time zone`
- **booking_end** `timestamp with time zone`
- **duration_min** `integer`
- **origin** `text`
- **price_amount** `numeric`
- **price_currency** `text`
- **booking_type** `text`
- **payment_status** `text`
- **status** `text`
- **is_canceled** `boolean` NOT NULL DEFAULT `false`
- **owner_id** `text`
- **coach_ids** `ARRAY`
- **course_id** `text`
- **course_name** `text`
- **activity_id** `text`
- **activity_name** `text`
- **raw_json** `jsonb`
- **synced_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `playtomic.payment_assignments`

> Asignación manual: 1 reserva Playtomic ↔ N pedidos de Waitry "Renta Cancha Padel" que la cubren. Audit trail nativo via assigned_by + assigned_at. UNIQUE(waitry_order_id) impide doble asignación. Iniciativa rdb-pagos-cancha-conciliacion.

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **booking_id** `text` NOT NULL — FK → `playtomic.bookings(booking_id)`
- **waitry_order_id** `text` NOT NULL — FK → `rdb.waitry_pedidos(order_id)`
- **assigned_amount** `numeric` NOT NULL
- **assigned_by** `uuid` NOT NULL
- **assigned_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **note** `text`

### `playtomic.payments_import`

> Pagos importados desde el reporte CSV de Playtomic Manager web. PRIMARY KEY payment_id permite UPSERT idempotente al re-subir el mismo periodo. Iniciativa rdb-pagos-cancha-conciliacion.

- **payment_id** `text` NOT NULL — PK
- **club_payment_id** `text`
- **refund_id** `text`
- **corporate_name** `text`
- **user_id** `text`
- **user_name** `text`
- **payout_code** `text`
- **sport** `text`
- **product_sku** `text`
- **origin** `text`
- **service_date** `timestamp with time zone`
- **payment_date** `timestamp with time zone`
- **payment_method** `text`
- **payment_type** `text`
- **payment_status** `text`
- **currency** `text`
- **total** `numeric`
- **subtotal** `numeric`
- **taxes** `numeric`
- **tax_rate** `numeric`
- **net_amount_transferred** `numeric`
- **b2b_fee_rate** `numeric`
- **b2b_fee_total** `numeric`
- **b2b_fee_subtotal** `numeric`
- **b2b_fee_taxes** `numeric`
- **b2b_fee_tax_rate** `numeric`
- **non_applicable_total** `numeric`
- **non_applicable_subtotal** `numeric`
- **non_applicable_taxes** `numeric`
- **invoice_id** `text`
- **invoice_number** `text`
- **invoice_date** `timestamp with time zone`
- **invoice_payer** `text`
- **store_product_name** `text`
- **store_product_quantity** `numeric`
- **campaign_id** `text`
- **campaign_name** `text`
- **uploaded_by** `uuid`
- **uploaded_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **source_filename** `text`

### `playtomic.players`

> Populated by edge function playtomic-sync using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **playtomic_id** `text` NOT NULL
- **name** `text`
- **email** `text`
- **player_type** `text`
- **accepts_commercial** `boolean`
- **first_seen_at** `timestamp with time zone`
- **last_seen_at** `timestamp with time zone`
- **total_bookings** `integer` NOT NULL DEFAULT `0`
- **total_spend** `numeric` NOT NULL DEFAULT `0`
- **favorite_sport** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `playtomic.resources`

> Populated by edge function playtomic-sync using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **resource_id** `text` NOT NULL
- **resource_name** `text`
- **sport_id** `text`
- **first_seen_at** `timestamp with time zone`
- **last_seen_at** `timestamp with time zone`
- **active** `boolean` NOT NULL DEFAULT `true`

### `playtomic.sync_log`

> Written by edge function playtomic-sync using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **sync_type** `text` NOT NULL
- **started_at** `timestamp with time zone`
- **finished_at** `timestamp with time zone`
- **status** `text` NOT NULL
- **bookings_fetched** `integer`
- **bookings_upserted** `integer`
- **players_upserted** `integer`
- **error_message** `text`
- **date_range_start** `timestamp with time zone`
- **date_range_end** `timestamp with time zone`

### `playtomic.v_bookings_payment_coverage` _(view)_

> Cobertura derivada por reserva: total reserva, suma de pagos asignados, estado (none/partial/full), %. Vista security_invoker — la RLS de la tabla subyacente aplica. Iniciativa rdb-pagos-cancha-conciliacion.

- **booking_id** `text`
- **booking_total** `numeric`
- **assigned_total** `numeric`
- **coverage_status** `text`
- **coverage_pct** `numeric`
- **assigned_waitry_orders** `ARRAY`

### `playtomic.v_bookings_total_coverage` _(view)_

> Cobertura combinada con breakdown por canal. Wallet usa non_applicable_total (monto real cubierto). Manager queda fuera del effective_total. Iniciativa rdb-pagos-cancha-conciliacion.

- **booking_id** `text`
- **booking_total** `numeric`
- **waitry_total** `numeric`
- **csv_total** `numeric`
- **combined_total** `numeric`
- **coverage_status** `text`
- **coverage_pct** `numeric`
- **waitry_order_ids** `ARRAY`
- **csv_payments_count** `bigint`
- **csv_payment_ids** `ARRAY`
- **online_csv_total** `numeric`
- **manager_csv_total** `numeric`
- **other_csv_total** `numeric`
- **wallet_payments_count** `bigint`
- **wallet_coverage** `numeric`
- **effective_total** `numeric`
- **effective_status** `text`
- **effective_pct** `numeric`
- **has_unverified_manager** `boolean`
- **online_csv_payment_ids** `ARRAY`
- **manager_csv_payment_ids** `ARRAY`
- **wallet_csv_payment_ids** `ARRAY`

### `playtomic.v_conciliacion_historial` _(view)_

> Eventos de cobertura unificados por booking: 1 fila por cada pago/asignación. Fuentes: waitry (manual), online (CSV App/Web), manager (CSV Playtomic Manager onsite). Iniciativa rdb-pagos-cancha-conciliacion (tab Historial).

- **row_id** `text`
- **source** `text`
- **booking_id** `text`
- **booking_start** `timestamp with time zone`
- **resource_name** `text`
- **booking_total** `numeric`
- **owner_id** `text`
- **reference_id** `text`
- **amount** `numeric`
- **payment_method** `text`
- **payment_origin** `text`
- **event_at** `timestamp with time zone`
- **assigned_by** `uuid`
- **subject** `text`
- **waitry_notes** `text`
- **waitry_paid_at** `timestamp with time zone`
- **waitry_order_total** `numeric`

### `playtomic.v_ocupacion_diaria` _(view)_

- **resource_name** `text`
- **fecha** `date`
- **hora** `integer`
- **reservas** `bigint`
- **revenue** `numeric`

### `playtomic.v_revenue_diario` _(view)_

- **fecha** `date`
- **sport_id** `text`
- **reservas** `bigint`
- **revenue** `numeric`
- **cancelaciones** `bigint`

### `playtomic.v_top_players` _(view)_

- **id** `uuid`
- **playtomic_id** `text`
- **name** `text`
- **email** `text`
- **player_type** `text`
- **accepts_commercial** `boolean`
- **first_seen_at** `timestamp with time zone`
- **last_seen_at** `timestamp with time zone`
- **total_bookings** `integer`
- **total_spend** `numeric`
- **favorite_sport** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`
- **reservas_periodo** `bigint`
- **gasto_estimado** `numeric`

---

## Schema `public`

### `public.health_ecg` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after /api/health/ingest migrates to supabase.schema('health').

- **id** `bigint`
- **date** `timestamp with time zone`
- **classification** `text`
- **heart_rate** `real`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone`

### `public.health_ingest_log` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after /api/health/ingest migrates to supabase.schema('health').

- **id** `bigint`
- **received_at** `timestamp with time zone`
- **payload_size_bytes** `integer`
- **metrics_count** `integer`
- **workouts_count** `integer`
- **source_ip** `text`
- **status** `text`
- **metrics_by_name** `jsonb`

### `public.health_medications` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after /api/health/ingest migrates to supabase.schema('health').

- **id** `bigint`
- **date** `timestamp with time zone`
- **name** `text`
- **dose** `text`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone`

### `public.health_metrics` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after /api/health/ingest migrates to supabase.schema('health').

- **id** `bigint`
- **metric_name** `text`
- **date** `timestamp with time zone`
- **value** `real`
- **unit** `text`
- **source** `text`
- **ingested_at** `timestamp with time zone`

### `public.health_workouts` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after /api/health/ingest migrates to supabase.schema('health').

- **id** `bigint`
- **name** `text`
- **start_time** `timestamp with time zone`
- **end_time** `timestamp with time zone`
- **duration_minutes** `real`
- **distance_km** `real`
- **energy_kcal** `real`
- **heart_rate_avg** `real`
- **heart_rate_max** `real`
- **source** `text`
- **raw_json** `jsonb`
- **ingested_at** `timestamp with time zone`

### `public.profile` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after callers migrate to core.profiles.

- **id** `uuid`
- **email** `text`
- **first_name** `text`
- **last_name** `text`
- **avatar_url** `text`
- **locale** `text`
- **is_active** `boolean`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`

### `public.user_presence` _(view)_

> TEMPORAL SHIM - drop on 2026-05-06 after callers migrate to core.user_presence.

- **user_id** `uuid`
- **email** `text`
- **display_name** `text`
- **avatar_url** `text`
- **current_path** `text`
- **current_module** `text`
- **last_seen_at** `timestamp with time zone`
- **status** `text`
- **created_at** `timestamp with time zone`

---

## Schema `rdb`

### `rdb.corte_conteo_denominaciones` _(view)_

> Compatibility shim over rdb.corte_conteo_denominaciones_legacy after ERP migration.

- **id** `uuid`
- **corte_id** `uuid`
- **denominacion** `numeric`
- **tipo** `text`
- **cantidad** `integer`
- **subtotal** `numeric`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`

### `rdb.corte_conteo_denominaciones_archive_2026_04_17`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **corte_id** `uuid` NOT NULL
- **denominacion** `numeric` NOT NULL
- **tipo** `text` NOT NULL
- **cantidad** `integer` NOT NULL DEFAULT `0`
- **subtotal** `numeric`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `rdb.ordenes_compra` _(view)_

> Compatibility shim over rdb.ordenes_compra_legacy after ERP migration.

- **id** `uuid`
- **folio** `text`
- **requisicion_id** `uuid`
- **proveedor_id** `uuid`
- **estatus** `text`
- **total_estimado** `numeric`
- **total_real** `numeric`
- **fecha_emision** `timestamp with time zone`
- **fecha_recepcion** `timestamp with time zone`
- **recibido_por** `uuid`
- **notas** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`

### `rdb.ordenes_compra_archive_2026_04_17`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **folio** `text` NOT NULL DEFAULT `''::text`
- **requisicion_id** `uuid` — FK → `rdb.requisiciones_archive_2026_04_17(id)`
- **proveedor_id** `uuid` — FK → `rdb.proveedores_archive_2026_04_17(id)`
- **estatus** `text` NOT NULL DEFAULT `'abierta'::text`
- **total_estimado** `numeric`
- **total_real** `numeric`
- **fecha_emision** `timestamp with time zone` DEFAULT `now()`
- **fecha_recepcion** `timestamp with time zone`
- **recibido_por** `uuid`
- **notas** `text`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **updated_at** `timestamp with time zone` DEFAULT `now()`

### `rdb.productos_waitry_map`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **producto_id** `uuid`
- **waitry_nombre** `text` NOT NULL
- **factor_salida** `numeric` DEFAULT `1.0`
- **created_at** `timestamp with time zone` DEFAULT `now()`

### `rdb.proveedores` _(view)_

> Compatibility shim over rdb.proveedores_legacy after ERP migration.

- **id** `uuid`
- **nombre** `text`
- **contacto** `text`
- **telefono** `text`
- **email** `text`
- **rfc** `text`
- **direccion** `text`
- **notas** `text`
- **activo** `boolean`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`

### `rdb.proveedores_archive_2026_04_17`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **nombre** `text` NOT NULL
- **contacto** `text`
- **telefono** `text`
- **email** `text`
- **rfc** `text`
- **direccion** `text`
- **notas** `text`
- **activo** `boolean` NOT NULL DEFAULT `true`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **updated_at** `timestamp with time zone` DEFAULT `now()`

### `rdb.requisiciones` _(view)_

> Compatibility shim over rdb.requisiciones_legacy after ERP migration.

- **id** `uuid`
- **folio** `text`
- **estatus** `text`
- **solicitado_por** `uuid`
- **aprobado_por** `uuid`
- **fecha_solicitud** `timestamp with time zone`
- **fecha_necesidad** `date`
- **notas** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`

### `rdb.requisiciones_archive_2026_04_17`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **folio** `text` NOT NULL DEFAULT `''::text`
- **estatus** `text` NOT NULL DEFAULT `'borrador'::text`
- **solicitado_por** `uuid`
- **aprobado_por** `uuid`
- **fecha_solicitud** `timestamp with time zone` DEFAULT `now()`
- **fecha_necesidad** `date`
- **notas** `text`
- **created_at** `timestamp with time zone` DEFAULT `now()`
- **updated_at** `timestamp with time zone` DEFAULT `now()`

### `rdb.v_categoria_resumen` _(view)_

- **categoria_id** `uuid`
- **categoria** `text`
- **color** `text`
- **orden** `integer`
- **total_productos** `bigint`
- **productos_con_venta_30d** `bigint`
- **importe_total_30d** `numeric`
- **utilidad_total_30d** `numeric`
- **margen_promedio_pct** `numeric`
- **valor_stock_total** `numeric`

### `rdb.v_corte_conteo_totales` _(view)_

- **corte_id** `uuid`
- **total_contado** `numeric`
- **detalle** `jsonb`

### `rdb.v_cortes_lista` _(view)_

- **id** `uuid`
- **corte_nombre** `text`
- **coda_id** `text`
- **caja_id** `uuid`
- **caja_nombre** `text`
- **fecha_operativa** `date`
- **hora_inicio** `timestamp with time zone`
- **hora_fin** `timestamp with time zone`
- **estado** `text`
- **turno** `text`
- **tipo** `text`
- **observaciones** `text`
- **efectivo_inicial** `numeric`
- **efectivo_contado** `numeric`
- **responsable_apertura** `text`
- **responsable_cierre** `text`
- **ingresos_efectivo** `numeric`
- **ingresos_tarjeta** `numeric`
- **ingresos_stripe** `numeric`
- **ingresos_transferencias** `numeric`
- **total_ingresos** `numeric`
- **depositos** `numeric`
- **retiros** `numeric`
- **efectivo_esperado** `numeric`
- **pedidos_count** `bigint`

### `rdb.v_cortes_productos` _(view)_

> Per-product aggregates per corte (RDB / Waitry POS). Joins via rdb.waitry_pedidos.corte_id FK. Created 2026-04-17 (B.1.extra.b) — replaces the archived definition that used the rdb.cortes ghost relation and a brittle timestamp-range join.

- **corte_id** `uuid`
- **product_id** `text`
- **producto_nombre** `text`
- **cantidad_vendida** `numeric`
- **importe_total** `numeric`

### `rdb.v_cortes_totales` _(view)_

> Totales por corte de caja RDB (Waitry). Excluye cancelados, pagos no completados (paid=false, ADR-035) y fantasmas (superseded_by_order_id, ADR-031/ADR-036). Vista no-materializada → corrige retroactivamente todos los cortes al recalcularse en cada lectura.

- **corte_id** `uuid`
- **empresa_id** `uuid`
- **caja_nombre** `text`
- **estado** `text`
- **hora_inicio** `timestamp with time zone`
- **hora_fin** `timestamp with time zone`
- **efectivo_inicial** `numeric`
- **ingresos_efectivo** `numeric`
- **ingresos_tarjeta** `numeric`
- **ingresos_stripe** `numeric`
- **ingresos_transferencias** `numeric`
- **total_ingresos** `numeric`
- **depositos** `numeric`
- **retiros** `numeric`
- **efectivo_esperado** `numeric`
- **pedidos_count** `bigint`
- **fecha_operativa** `date`

### `rdb.v_inventario_stock` _(view)_

- **id** `uuid`
- **nombre** `text`
- **categoria** `text`
- **clasificacion** `text`
- **unidad** `text`
- **inventariable** `boolean`
- **costo_unitario** `numeric`
- **ultimo_costo** `numeric`
- **stock_minimo** `integer`
- **factor_consumo** `numeric`
- **total_entradas** `numeric`
- **total_vendido** `numeric`
- **total_mermas** `numeric`
- **stock_actual** `numeric`
- **valor_inventario** `numeric`
- **bajo_minimo** `boolean`

### `rdb.v_producto_metricas` _(view)_

- **id** `uuid`
- **nombre** `text`
- **codigo** `text`
- **activo** `boolean`
- **inventariable** `boolean`
- **categoria_id** `uuid`
- **categoria_nombre** `text`
- **categoria_color** `text`
- **costo** `numeric`
- **precio_venta** `numeric`
- **margen_pct** `numeric`
- **stock_actual** `numeric`
- **valor_stock** `numeric`
- **unidades_30d** `numeric`
- **importe_30d** `numeric`
- **unidades_90d** `numeric`
- **importe_90d** `numeric`
- **ultima_venta_at** `timestamp with time zone`
- **dias_sin_venta** `integer`
- **utilidad_30d** `numeric`

### `rdb.v_producto_tendencia_semanal` _(view)_

- **producto_id** `uuid`
- **nombre** `text`
- **categoria_id** `uuid`
- **semana_inicio** `date`
- **unidades** `numeric`
- **importe** `numeric`

### `rdb.v_producto_ultima_venta` _(view)_

- **producto_id** `uuid`
- **ultima_venta_at** `timestamp with time zone`
- **total_ventas** `bigint`
- **total_unidades_vendidas** `numeric`
- **total_importe_vendido** `numeric`

### `rdb.v_productos_grupo` _(view)_

- **padre_id** `uuid`
- **padre_nombre** `text`
- **categoria** `text`
- **costo_unitario** `numeric`
- **unidad** `text`
- **total_hijos** `integer`
- **hijos** `jsonb`

### `rdb.v_productos_tabla` _(view)_

- **id** `uuid`
- **codigo** `text`
- **nombre** `text`
- **descripcion** `text`
- **tipo** `text`
- **unidad** `text`
- **activo** `boolean`
- **inventariable** `boolean`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`
- **categoria_id** `uuid`
- **categoria_nombre** `text`
- **categoria_color** `text`
- **ultimo_costo** `numeric`
- **ultimo_precio_venta** `numeric`
- **margen_pct** `numeric`
- **stock_actual** `numeric`
- **ultima_venta_at** `timestamp with time zone`
- **total_unidades_vendidas** `numeric`

### `rdb.v_waitry_pedidos` _(view)_

> Vista canónica de pedidos Waitry: excluye fantasmas (superseded_by_order_id, ADR-031) y pagos no completados (paid=false → venta no hecha, ADR-035). Default para todos los reads de UI/reportes/conciliación/inventario. Para auditoría con fantasmas + intentos fallidos, leer rdb.v_waitry_pedidos_con_fantasmas o la tabla base.

- **id** `uuid`
- **order_id** `text`
- **status** `text`
- **paid** `boolean`
- **timestamp** `timestamp with time zone`
- **place_id** `text`
- **place_name** `text`
- **table_name** `text`
- **layout_name** `text`
- **total_amount** `numeric`
- **total_discount** `numeric`
- **service_charge** `numeric`
- **tax** `numeric`
- **external_delivery_id** `text`
- **notes** `text`
- **last_action_at** `timestamp with time zone`
- **content_hash** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`
- **corte_id** `uuid`
- **table_id** `integer`
- **superseded_by_order_id** `text`
- **es_fantasma** `boolean`

### `rdb.v_waitry_pedidos_con_fantasmas` _(view)_

> Vista auditable de pedidos Waitry: incluye TODOS los pedidos (canónicos + fantasmas) con flag es_fantasma. Para toggle "Mostrar duplicados detectados" en /rdb/ventas y para QA contra cortes históricos. NO usar en reportes financieros — usar rdb.v_waitry_pedidos.

- **id** `uuid`
- **order_id** `text`
- **status** `text`
- **paid** `boolean`
- **timestamp** `timestamp with time zone`
- **place_id** `text`
- **place_name** `text`
- **table_name** `text`
- **layout_name** `text`
- **total_amount** `numeric`
- **total_discount** `numeric`
- **service_charge** `numeric`
- **tax** `numeric`
- **external_delivery_id** `text`
- **notes** `text`
- **last_action_at** `timestamp with time zone`
- **content_hash** `text`
- **created_at** `timestamp with time zone`
- **updated_at** `timestamp with time zone`
- **corte_id** `uuid`
- **table_id** `integer`
- **superseded_by_order_id** `text`
- **es_fantasma** `boolean`

### `rdb.v_waitry_pedidos_reversa_sospechosa` _(view)_

- **order_id** `text`
- **timestamp** `timestamp with time zone`
- **status** `text`
- **place_name** `text`
- **layout_name** `text`
- **table_name** `text`
- **corte_id** `uuid`
- **total_amount** `numeric`
- **total_discount** `numeric`
- **tax** `numeric`
- **service_charge** `numeric`
- **product_lines** `bigint`
- **product_qty** `numeric`
- **product_total** `numeric`
- **products_summary** `text`
- **mesero_nombre** `text`
- **pos_nombre** `text`
- **pos_username** `text`
- **pos_email** `text`
- **pos_user_id** `text`
- **positive_payment_count** `bigint`
- **negative_payment_count** `bigint`
- **total_positive_amount** `numeric`
- **total_negative_amount** `numeric`
- **net_payments** `numeric`
- **positive_methods** `ARRAY`
- **negative_methods** `ARRAY`
- **payments_summary** `text`
- **matched_pair_count** `bigint`
- **matched_amount_total** `numeric`
- **matched_pairs** `jsonb`
- **anomaly_type** `text`
- **is_zero_net** `boolean`
- **is_partial_net** `boolean`
- **looks_like_unmarked_cancellation** `boolean`

### `rdb.v_waitry_pending_duplicates` _(view)_

- **id** `uuid`
- **detected_at** `timestamp with time zone`
- **similarity_score** `numeric`
- **match_reason** `text`
- **content_hash** `text`
- **order_id_a** `text`
- **order_a_timestamp** `timestamp with time zone`
- **order_a_place_name** `text`
- **order_a_table_name** `text`
- **order_a_total_amount** `numeric`
- **order_a_status** `text`
- **order_a_notes** `text`
- **order_id_b** `text`
- **order_b_timestamp** `timestamp with time zone`
- **order_b_place_name** `text`
- **order_b_table_name** `text`
- **order_b_total_amount** `numeric`
- **order_b_status** `text`
- **order_b_notes** `text`
- **seconds_apart** `numeric`

### `rdb.v_waitry_productos_categoria` _(view)_

> Líneas de venta Waitry (rdb.waitry_productos) enriquecidas con producto de catálogo y categoría. Enlace por waitry_productos.product_id = erp.productos.codigo, desambiguado con DISTINCT ON para los códigos duplicados. Líneas sin match traen categoria_id NULL. Usada por el tab "Por categoría" de /rdb/ventas (iniciativa rdb-ventas-por-categoria).

- **id** `uuid`
- **order_id** `text`
- **product_id** `text`
- **product_name** `text`
- **quantity** `numeric`
- **unit_price** `numeric`
- **total_price** `numeric`
- **created_at** `timestamp with time zone`
- **producto_catalogo_id** `uuid`
- **categoria_id** `uuid`
- **categoria_nombre** `text`
- **categoria_color** `text`
- **categoria_orden** `integer`

### `rdb.waitry_duplicate_candidates`

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **order_id_a** `text` NOT NULL
- **order_id_b** `text` NOT NULL
- **similarity_score** `numeric` NOT NULL
- **match_reason** `text`
- **content_hash** `text` NOT NULL
- **detected_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **resolved** `boolean` NOT NULL DEFAULT `false`
- **resolution** `text`

### `rdb.waitry_inbound`

> Populated by waitry-webhook edge function using service_role (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **order_id** `text` NOT NULL
- **event** `text`
- **payload_json** `jsonb` NOT NULL
- **payload_hash** `text` NOT NULL
- **received_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **processed** `boolean` NOT NULL DEFAULT `false`
- **attempts** `integer` NOT NULL DEFAULT `0`
- **error** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `rdb.waitry_pagos`

> Derived from waitry_inbound trigger; service_role writes (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **order_id** `text` NOT NULL — FK → `rdb.waitry_pedidos(order_id)`
- **payment_id** `text`
- **payment_method** `text`
- **amount** `numeric`
- **tip** `numeric`
- **currency** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`

### `rdb.waitry_pedidos`

> Derived from waitry_inbound trigger; service_role writes (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **order_id** `text` NOT NULL
- **status** `text`
- **paid** `boolean`
- **timestamp** `timestamp with time zone`
- **place_id** `text`
- **place_name** `text`
- **table_name** `text`
- **layout_name** `text`
- **total_amount** `numeric`
- **total_discount** `numeric`
- **service_charge** `numeric`
- **tax** `numeric`
- **external_delivery_id** `text`
- **notes** `text`
- **last_action_at** `timestamp with time zone`
- **content_hash** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **updated_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
- **corte_id** `uuid` — FK → `erp.cortes_caja(id)`
- **table_id** `integer`
- **superseded_by_order_id** `text`

### `rdb.waitry_productos`

> Derived from waitry_inbound trigger; service_role writes (bypasses RLS).

- **id** `uuid` NOT NULL DEFAULT `gen_random_uuid()` — PK
- **order_id** `text` NOT NULL — FK → `rdb.waitry_pedidos(order_id)`
- **product_id** `text`
- **product_name** `text` NOT NULL
- **quantity** `numeric`
- **unit_price** `numeric`
- **total_price** `numeric`
- **modifiers** `jsonb`
- **notes** `text`
- **created_at** `timestamp with time zone` NOT NULL DEFAULT `now()`
